# 아구구(a99) 작업 인수인계 — 2026-07-23 (최신)

지도/광고/SEO/브랜딩/신규기능 작업 기록. 다음 작업자(또는 미래의 나)를 위한 정리.
※ 이전 버전(2026-07-21)의 내용을 모두 포함하고 그 뒤 작업을 추가함. 이 파일이 최신.

---

## 1. 광고 (AdFit)

- **반려 원인 제거** — 지도 우측 하단 "닫기형" 코너 광고 삭제. 광고 위 "닫기" 버튼이 오클릭 유발로 매체 심사 반려였음 → 삭제 후 **매체 승인**됨.
- **아파트 상세페이지(`api/aptpage.js`)**: 좌·우 사이드 160×600 + 하단 300×250 + **최상단 얇은 배너(320×100, `AD_UNIT_TOP` = DAN-MS50 재사용)**.
  - 최상단 배너: `<body>` 바로 아래 `.top-ad-bar` 전체폭 정적 `ins`. 나중에 PC 728×90 만들면 기기별 분기 가능.
- **검색 페이지(`index.html`)**: 좌·우 160×600 + 하단 300×250(페이지번호 위, 여백 28px).
- **지도 상세 상단 PC**: 죽은 250×250 → 새 300×250(`DAN-W27gieBwJhhY4OlP`). 모바일은 320×100.
- **광고 사라짐 버그 수정**: `a99LoadAd()` 헬퍼 — 새 `ba.min.js` 로드 직전 이미 렌더된 `ins`의 class를 `kakao_ad_done`으로 바꿔 스캔에서 제외(보호). 지도→검색 이동 시 사이드 광고 사라지던 문제 해결.

### AdFit 광고 단위 (전부 "승인")

| 코드 | 사이즈 | 사용처 |
|---|---|---|
| DAN-A9WnwICFDEl16jU1 | 160×600 | 아파트 좌 사이드 + 검색 좌 |
| DAN-6hzBUhx1qcJKOIsS | 160×600 | 아파트 우 사이드 + 검색 우 |
| DAN-VVNFKrpvthFpw6PS | 300×250 | 아파트 하단 + 검색 하단 (본문배너) |
| DAN-KpmCmBSL2hBTN2xb | 300×250 | 지도 상세 하단 |
| DAN-W27gieBwJhhY4OlP | 300×250 | 지도 상세 상단 PC |
| DAN-MS50ssMpQLYwDL5r | 320×100 | 지도 상세 상단 모바일 + 아파트페이지 최상단 배너 |
| DAN-2U9jwSfPfCYLIyyE | 250×250 | **폐기** (fill 안 됨, 코드에서 제거) |

### 광고 gotchas
- **단위 삭제 금지** (여러 페이지 재사용 중). **같은 DOM에 같은 단위 두 번 금지**(다른 페이지 재사용은 OK).
- **250×250 쓰지 말 것**(fill 안 됨). 표준 사이즈(300×250/160×600/320×100/728×90) 사용.
- **새 단위는 실광고/대체광고 뜨는 데 몇 시간~하루** 걸림.
- **광고에 닫기(X) 버튼 금지**(심사 반려). **본인 클릭 금지**(부정클릭 정지).
- "카카오 비즈니스"만 뜨는 건 정상(no-fill 대체광고).
- **새 광고 슬롯 추가 시 반드시 `a99LoadAd()` 패턴** (안 그러면 사라짐 버그 재발).

---

## 2. SEO / 색인

- **홈페이지 메타**(`index.html`): description(80자↓), canonical, og:*, twitter card.
- **브랜딩 a99 → 아구구 통일**(`api/aptpage.js`): 로고 `아구구`, `og:site_name`=아구구, 푸터. `index.html` og:site_name도 아구구.
- **구조화 데이터(JSON-LD)** 단지페이지에 추가: `WebSite`(name 아구구), `BreadcrumbList`(아구구 › 단지명) → 검색결과에 "아구구" 브랜드/브레드크럼 노출 유도.
- **사이트맵 (규격 정비)**:
  - `apt/sitemap.xml` — 전 단지 + `lastmod`
  - `sitemap-main.xml` — 홈/privacy/terms
  - `sitemap.xml` (root) — **사이트맵 인덱스**(위 둘을 묶음)
  - `robots.txt` → root `sitemap.xml` 가리킴
  - 생성기: `node scripts/build_sitemap.js` (데이터 갱신 시 재실행 → lastmod 갱신 → 재수집 유도)
- **단지페이지 내부링크**: "주변 아파트 단지"(같은 동→시군구, 12개) → 크롤러 순회·색인 가속. 앵커 텍스트 "○○ 관리사무소 전화번호"(키워드 매칭).
- **IndexNow (네이버·빙 즉시 색인)** — 신규:
  - 인증키 파일: `3c4fc034da972bac20f024ef003bf1ec.txt` (루트)
  - 제출 엔드포인트: `api/indexnow.js` — `/api/indexnow?secret=<키>[&limit=N]` 방문하면 전 URL을 IndexNow API에 배치 제출. **데이터 추가·수정 시 이 URL 다시 열면 재제출.** (구글은 IndexNow 미지원 → 사이트맵+서치콘솔)
- **서치어드바이저 제출**: 사이트맵을 `https://a99.co.kr/sitemap.xml`(인덱스)로. 웹페이지 수집은 홈+대표 단지 위주.

---

## 3. 기능/UI

- **F5 탭 유지**: 검색/계산기/PRO에 hash 라우팅(`#search` 등). `showPage`에서 replaceState, onload에서 복원.
- **핀 로딩 속도**: 지도 초기화를 `DOMContentLoaded` 즉시 실행(광고 안 기다림). `initMap`은 `if(map)return` 가드.
- **지도 검색창 제거**: "단지명, 주소 검색"(PC 상단바 + 모바일 🔍) 삭제. (검색은 상단 "아파트검색" 탭 사용)
- **지도 상단 필터바(신규, 1단계)** — 검색창 자리에:
  - 작동: 유형(아파트/오피스텔) 칩, 세대수·면적·준공 드롭다운, 명칭↔세대수 핀 라벨 토글, 초기화
  - 준비중(표시만, `mfSoon()` 토스트): 분양권/재건축/재개발, 가격대, 거래방식
  - 필터 로직: `passMapFilter(d)` → `renderVisibleMarkers`/`renderMarkers` predicate. 라벨 토글: `pinLabelMode`로 `addMarker` 분기.
  - CSS 접두어 `.mfbar/.mfc/.mfd/.mflbl`. 바 폭 `100vw-70px`(스크롤바 숨김).
  - **모바일엔 아직 없음**(map-top-bar 모바일 숨김). 2단계 예정.
- **헤더 오른쪽**: "PRO 중개사" 버튼 → **아이콘+라벨 메뉴 4개**(집내놓기→openSellModal, 중개사→showPage('pro'), 단지제보→openReportModal, 이용안내→openGuideModal). `이용안내` 모달 신규 추가. 모바일엔 숨김(☰ 메뉴 유지).
- **실거래가 차트 툴팁**: 반올림("4.8억") → **전체 금액("4억 8,350만원")**. `fmtPFull()` 추가, 매매/전세/보증금에 적용. Y축은 짧게 유지.
- **파비콘**: 초록 라운드+흰 집. `favicon.ico/.svg/.png(48/180/192/512)`. `index.html`+`aptpage.js`에 link 태그. 삼색 빌딩 대안본 보관: `favicon-alt-buildings.svg/.png`. (버전쿼리 `?v=`는 미적용 — 나중에 교체 편하려면 붙이면 됨)

---

## 4. 데이터

- **가짜 데이터 9개 삭제**(test/테스트/한국감정원 계열) — `split_output/coords_all_apt.json`, `coords_apt_11530/11680/27140/41281.json`, `kapt_tel.json`에서 제거. **전체 단지 21,727 → 21,718**. 사이트맵 재생성 완료.
- **관리사무소 전화번호**: 21,690개 있음(99.8%). **진짜로 없는 28개**는 소형 단지라 웹에 잘 안 나옴 → "준비중" 표시로 두고 수집은 급하지 않음. (지도 데이터는 브라우저에 직접 서빙되므로 비공개 불가 — repo private 의미 적음)

---

## 5. 배포/운영/비용

- **git push → Vercel 자동 배포.** 도메인 https://a99.co.kr.
- **`vercel.json`**: `/apt/:code`→`/api/aptpage` rewrite, `/split_output/*` 캐시 헤더, **비-a99 호스트(예: *.vercel.app) → a99.co.kr 308 리다이렉트**(중복 URL 방지). ※ 프리뷰 배포 주소도 리다이렉트됨.
- GA4 `G-B2NJPFNP69`. 네이버·구글 인증 메타 있음.
- **비용**: GitHub 무료, Vercel 무료(Hobby — 상업용이라 트래픽·수익 커지면 Pro $20/월 고려), 도메인 연 1~2만원. 영상은 자체 호스팅 X.
- **해외(프랑스 등) 트래픽 = 대부분 봇.** 광고 안 뜨면 대개 방문자 PC의 보안/애드블록(사이트 문제 아님).

---

## 6. 다음 할 일 / 아이디어 (미구현)

1. **단지페이지 콘텐츠 보강 + FAQ 구조화 데이터** — 얇은 페이지 두껍게(실거래 요약·주변정보·Q&A). 검색 순위·연관검색에 제일 효과 큼. (SEO 핵심 다음 스텝)
2. **지역 허브 페이지**(시도→시군구→단지 목록) — 크롤러 순회 가속 + 중간 검색어("○○시 아파트 관리사무소 번호") 공략.
3. **매물 영상(유튜브 임베드)** — 데모 완성(`youtube_embed_demo.html`, 아웃풋 폴더). 링크만 넣으면 재생. 영상은 유튜브가 호스팅(비용 0). 단지/매물 페이지에 붙이는 것부터 시작 가능. CDN(Cloudflare Stream 등)은 나중.
4. **백링크**(지식iN·부동산 카페) — 네이버 신뢰도·연관검색 씨앗. (사람이 직접)
5. **번호 없는 28개** 수집(급하지 않음).

### ★ 중개사 플랫폼 (제대로 만들 큰 프로젝트 — 나중에)

> 사장님 결정: "첨부터 제대로 만든다." → MVP(계정 없이)로 둘러가지 말고 계정·DB 기반으로 정식 구축.
> **정적 사이트 → 계정·DB 있는 앱**으로 올라가는 단계. 몇 주짜리 프로젝트.

**만들 것:**
- 중개사 **회원가입/로그인** + **자격 인증**(공인중개사만 — 사업자번호 확인/수동 승인)
- **중개사 개인 페이지** `/pro/{중개사}` (프로필+매물+영상+연락처, 서버렌더 → SEO)
- **매물 관리**(등록·수정·삭제) + 사진 업로드
- **매물안내서** 2종:
  - **프린트용** (사진 O) — **이미 있음**(PRO 페이지 "📄 매물 안내서" 탭, 새 창 인쇄, 사진 최대 5장). 손대지 말 것.
  - **링크 공유용** (신규) — 매물 여러 개 묶어 **짧은 링크** 생성 → 카톡·문자 전송 → 손님 화면 + **광고**.
    - (계정 없이 하는 초간단 버전은 데이터를 링크에 base64로 담으면 DB 없이도 됨. 근데 사장님은 정식 DB 버전 원함.)
- 손님 대면 화면 **광고**(애드핏) = 수익화. 중개사 참여동기 = 손님한테 매물 예쁘게 보내는 도구.
- **관리자 검수**(중개사 승인·매물 검토)

**추천 스택: Supabase(DB+로그인+사진저장, 무료 시작 → 월 $25) + Vercel(서버렌더).**

**빌드 순서:** ①Supabase 세팅·DB 스키마(중개사/매물/사진) → ②가입·로그인·자격인증 → ③중개사 대시보드(매물 CRUD+사진) → ④중개사 공개 페이지(서버렌더+광고) → ⑤링크 공유(짧은링크+손님화면+광고) → ⑥관리자 검수.

**시작 전 준비(사장님):** Supabase 계정·프로젝트 생성 → API 키. 그거 주면 코드 연결. (사진은 영상과 달리 작아서 무료 호스팅으로 충분 — 영상만 유튜브)

**참고:** 지금도 PRO 중개사 구독(월 7,700/19,900원, 매물안내서 장수 제한)이라는 형태로 중개사 구분은 있음. 계정 붙이면 여기에 통합.

---

## 6.5 실거래가 캐시 + 워밍 시스템 (이번에 점검·구축)

**구조:** 국토부 실거래가 API(매매/전월세/분양권) → `api/trade.js` → **Supabase `apt_transactions`** 캐시.

- **TTL 3단계**(거래취소 뒤늦은 반영 대응): 최근 2개월 24h / 2~7개월 7일 / 7개월+ 영구.
- **취소(계약해제)**: `cdealType` 있으면 fetch 시 제외. 뒤늦은 취소는 2~7개월 7일 TTL로 재확인해 반영.
- **효율**: 과거(7개월+)는 영구 캐시(재요청 X). 조회 시 최신 달만 땡김. **하루 호출 ≈ (그날 조회된 단지 수) × 8콜**. 전체 21k 매일 아님.

**★ 발견·해결한 이슈:** Supabase **무료 플랜 자동 일시정지**(7일 무활동)로 캐시가 한동안 죽어있었음 → **Resume**로 해결. 재발 방지 **`api/keepalive.js` + Vercel Cron(매일)** 추가.

**캐시 워밍(전국 미리 채우기):**
- `api/warm.js` — 시군구(lawd_cd)+월 단위로 국토부 받아 그 시군구 모든 단지에 매칭·저장. 진행상황 `warm_state`(id=1, cursor)에 기록, 하루 국토부 호출 캡(9000). 매칭 로직은 trade.js 이식. 전국 6,048태스크(252시군구×24개월) ≈ 2~3일.
- **트리거**: `.github/workflows/warm.yml` — GitHub Actions가 20분마다 `/api/warm?secret=...` 반복 호출(외부 가입 불필요). `done`/`paused`면 종료. **완료 후 이 워크플로우 삭제 가능.**
- **필요 사전작업**: Supabase SQL로 `warm_state` 테이블 생성(RLS 없이). 
  ```sql
  create table if not exists warm_state (id int primary key, cursor int default 0, calls_today int default 0, day date);
  ```
- **진행률 확인**: `https://a99.co.kr/api/warm?secret=3c4fc034da972bac20f024ef003bf1ec` → `"percent"`.
- secret = IndexNow 키와 동일(`3c4fc034...`). 워밍 엔드포인트는 국토부 캐시만 채우므로 노출돼도 위험 낮음(하루 캡 있음).

**나중 스케일 대비(미구현):** 트래픽 커지면 (1) data.go.kr **운영계정**으로 한도 상향, 또는 (2) trade.js 갱신을 **시군구 단위로** 바꿔 하루 호출을 시군구 수(≤252)에 묶기 → 하루 ~2,000콜 고정.

**Supabase 환경변수**: `SUPABASE_URL`, `SUPABASE_KEY`(Vercel Production에 등록됨, 5/23). `apt_transactions`는 `(kapt_code, ym)` 유니크 제약 있음(upsert 정상 동작 확인).

---

## 7. 주요 파일 지도

- `index.html` — 지도 SPA(지도/검색/계산기/PRO). 필터바·헤더·차트·모달·마커 로직.
- `api/aptpage.js` — 단지 상세페이지 서버렌더(`/apt/{code}`). 메타·JSON-LD·광고·주변단지.
- `api/indexnow.js` — IndexNow 제출 엔드포인트.
- `scripts/build_sitemap.js` — 사이트맵 생성. `scripts/build_seo_pages.js` — 정적 단지 html 생성기(레거시, 서빙은 aptpage.js가 함).
- `split_output/*.json` — 단지 좌표·정보 + `kapt_tel.json`(전화번호).
- `vercel.json`, `robots.txt`, `sitemap.xml`/`sitemap-main.xml`/`apt/sitemap.xml`, 파비콘들, 인증키 txt.
