# 아구구(a99) 작업 인수인계 — 2026-07-21

지도/광고/SEO 관련 작업 기록. 다음 작업자(또는 미래의 나)를 위한 정리.

---

## 1. 이번 세션에 변경한 것

### 광고 (AdFit)
1. **반려 원인 제거** — `index.html`: 지도 우측 하단 "닫기형" 코너 광고(`corner-ad`) 블록 통째 삭제.
   - 광고 위 "닫기" 버튼이 **오클릭 유발**로 애드핏 매체 심사 반려 사유였음. 삭제 후 매체 승인됨.
2. **아파트 상세페이지 좌측 사이드 광고 추가** — `api/aptpage.js`: 우측 160×600에 더해 좌측 160×600 추가 (PC ≥1040px만).
3. **검색 페이지 광고 3개 추가** — `index.html`: 좌·우 160×600 + 하단 300×250.
   - 하단 광고는 **검색결과 아래·페이지번호 위**에 배치, 위아래 여백(28px)으로 오클릭 방지.
   - `loadSearchAds()`로 검색 탭 열릴 때 로드.
4. **지도 상세 상단 PC 광고 교체** — `index.html`: 죽은 250×250(`DAN-2U9jwSfPfCYLIyyE`, fill 안 됨) → 새 300×250(`DAN-W27gieBwJhhY4OlP`). 모바일은 320×100 유지.
5. **광고 사라짐 버그 수정** — `index.html`: 지도→검색 이동 시 새 `ba.min.js` 스캔이 숨겨진 사이드바 광고를 지우던 문제.
   - `a99LoadAd()` 헬퍼 추가: 스크립트 로드 직전 **이미 렌더된 광고(`ins`)의 class를 `kakao_ad_done`으로 바꿔 스캔 대상에서 제외** → 기존 광고 보호.
   - `loadSidebarAd()` / `loadSearchAds()`가 스크립트를 그룹당 1번만 로드하도록 정리.

### 기능/성능
6. **F5 새로고침 시 탭 유지** — `index.html`: 검색/계산기/PRO 탭에 hash 라우팅(`#search` 등).
   - `showPage()`에서 `history.replaceState`, `window.onload`에서 hash 읽어 복원.
7. **핀박스 로딩 속도 개선** — `index.html`: 지도 초기화를 `window.onload`(광고·이미지 다 기다림)+500ms → **`DOMContentLoaded` 즉시 실행**으로 변경. (`initMap`은 `if(map)return` 가드로 중복 안전)

### SEO
8. **홈페이지 메타 추가** — `index.html` `<head>`: `description`(80자 이내), `canonical`, `og:type/title/description/url/site_name`, twitter card.
   - 네이버 서치어드바이저 URL 진단의 ❌ 3개(사이트 설명·OG 제목·OG 설명) 해결.

---

## 2. AdFit 광고 단위 목록 (전부 "승인")

| 코드 | 사이즈 | 사용처 |
|---|---|---|
| DAN-A9WnwICFDEl16jU1 | 160×600 | 아파트 좌 사이드 + 검색 좌 |
| DAN-6hzBUhx1qcJKOIsS | 160×600 | 아파트 우 사이드 + 검색 우 |
| DAN-VVNFKrpvthFpw6PS | 300×250 | 아파트 하단 + 검색 하단 (본문배너) |
| DAN-KpmCmBSL2hBTN2xb | 300×250 | 지도 상세 하단 |
| DAN-W27gieBwJhhY4OlP | 300×250 | 지도 상세 상단 PC (신규) |
| DAN-MS50ssMpQLYwDL5r | 320×100 | 지도 상세 상단 모바일 |
| DAN-2U9jwSfPfCYLIyyE | 250×250 | **폐기** (fill 안 됨, 코드에서 제거) |

---

## 3. 중요 주의사항 (gotchas)

- **애드핏 단위 삭제 금지** — 여러 페이지에서 재사용 중. 삭제하면 해당 광고 다 죽음.
- **같은 페이지(같은 DOM) 안에 같은 단위 두 번 넣으면 충돌** → 하나만 뜨거나 오작동. 단, 다른 페이지(aptpage.js vs index.html)에 같은 단위 재사용은 OK.
- **250×250은 fill 안 됨** (광고주·카카오 대체광고 둘 다 없음) → 쓰지 말 것. 표준 사이즈(300×250, 160×600, 320×100) 사용.
- **새 애드핏 단위는 실광고/대체광고 뜨는 데 몇 시간~하루** 걸림. 바로 안 떠도 정상.
- **광고에 닫기(X) 버튼 붙이지 말 것** — 오클릭 유발로 심사 반려됨.
- **본인 광고 클릭/반복 노출 금지** — 부정클릭으로 계정 정지 위험.
- 광고가 안 뜨고 "카카오 비즈니스"만 뜨는 건 정상(실광고 no-fill 시 대체광고). 표준 사이즈에서만 나옴.

---

## 4. 미완료 / 다음 할 일 (SEO 강화)

1. **사이트맵에 `lastmod` 추가 + 홈페이지·검색페이지 포함** — 현재 `apt/sitemap.xml`은 /apt/ 2만여 개만, `lastmod` 없음. 크롤러 재수집 판단용으로 필요.
2. **단지 상세페이지끼리 "주변 단지" 내부 링크** — 크롤러가 2만 페이지를 잘 타고 다니게.
3. **실거래가 데이터 갱신 시 사이트맵 자동 재생성 + 서치어드바이저 재제출**.
4. **부동산 카페/블로그 백링크** — 네이버는 자사 생태계 우대라 외부 유입·신뢰도 확보 필요.

---

## 5. 배포/운영 메모

- 변경사항은 **git push → Vercel 자동 배포**.
- 도메인: https://a99.co.kr (`vercel.json`에서 `/apt/:code` → `/api/aptpage` rewrite).
- GA4: `G-B2NJPFNP69`. 네이버 인증: `naver-site-verification` 메타 있음. 서치어드바이저 사이트맵 제출·수집요청 완료함.
- **해외 트래픽(프랑스 등)은 대부분 봇/크롤러** — 실사용자 아님. 신규 사이트라 봇 비율이 상대적으로 큼.
- 애드핏 광고 로딩 구조: 슬롯 `div`는 비어 있고 JS(`loadSidebarAd`/`loadSearchAds`)가 `ins` 주입 후 `a99LoadAd()`로 스크립트 로드. 새 광고 슬롯 추가 시 **반드시 `a99LoadAd()` 패턴 따를 것** (안 그러면 4·5번 버그 재발).
