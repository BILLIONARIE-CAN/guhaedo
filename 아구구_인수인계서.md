# 🏠 아구구 플랫폼 인수인계서 (LIVING DOC)

최종 갱신: 2026-09-05  갱신 주체: Claude (세션 인수자)  버전: v22

---

## 🔁 인수자 안내 (READ FIRST)

이 문서는 세션 간 인수인계용 살아있는 문서입니다.  
새 세션 시작 시 이 파일을 붙여넣고 "이어서 작업해줘"라고 하면:
1. 전체 읽고 현재 상태 파악
2. 요청 작업 수행
3. 작업 끝나면 이 문서 갱신 (맨 아래 지시문 참조)

⚠️ 비밀키/관리자 비번 포함 — GitHub 공개 레포 커밋 금지

---

## 📍 재개 지점 (2026-09-01, 앱 재설치 직전 기록)

> 사용자가 Cowork 워크스페이스 VM 서비스 다운(bash 실행 불가, 파일편집·브라우저는 정상)으로 **앱 재설치 예정**. 채팅 유실 대비해 여기 남김. 새 세션은 이 문서 붙여넣고 "이어서" 하면 됨.

**지금 상태: 코드는 전부 커밋·푸시 완료, 라이브 배포됨. 미푸시 없음.**

**사용자가 할 일 (대기 중):**
1. ⬜ **build_pyeong.sql / build_metrics.sql 재실행** — Supabase SQL Editor에 각각 붙여넣고 Run (재워밍 실거래를 평형·시세에 반영). 아직 안 함. 두 파일 `scripts/` 에 있음.
2. ⬜ **애드센스 심사 결과 대기** — pub-3915435017395988, 사이트 a99.co.kr 검토요청 제출 완료(2026-08-31). ads.txt·인증코드·head 스니펫 다 라이브. 며칠~2주. 승인 시 → 수동 광고단위 만들어 index/aptpage에 배치(애드핏과 공존).
3. ⬜ (선택) 애드센스 GDPR 동의메시지 — 사이트연결+개인정보 URL까지 했고 **로고 필요해서 게시 보류**(초안 저장). 승인·수익 무관, 나중에.
4. ⬜ (선택) IndexNow 첫 수동 제출 — 배포됐으니 `a99.co.kr/api/indexnow?secret=3c4fc034da972bac20f024ef003bf1ec` 한 번 열면 네이버·빙 대량제출(이후 주간크론 자동).

**색인 현황(참고):** 구글 색인 691 / 미색인 21,620(≈3%) — 오늘 실거래삽입 재크롤 대기. 네이버 크롤 정상(하루 500~640). 우선단지 있으면 GSC URL 색인요청(하루 10~20).

**환경 이슈:** 워크스페이스 VM(bash) 다운 → git 커밋/스크립트 실행 불가. 코드 수정은 파일편집으로 가능, 커밋·푸시는 사용자가 GitHub Desktop으로. VM은 앱 업데이트/컴퓨터 재시작/가상화(WSL) 확인으로 복구.

---

---

## 🆕 v22 (2026-09-05) — 지도 이동 버그 근본원인 + git 락파일 근본원인

### 1. 🔴 다른 시/도 선택해도 지도가 이전 위치에 머무름 (수정 완료)

**증상(사용자 보고):** "서울 강남구 누르면 첨엔 정상인데, 인천 연수구 누르면 다시 강남구로 나와"

**오진했던 방향:** 처음엔 사이드박스 목록 데이터 폴백 문제로 봤음. 실제로 목록은 매번 정확했음(라이브 추적으로 확인).

**진짜 원인 — 카카오맵 API 특성:**
```js
map.panTo(latlng);   // 애니메이션 이동 시작
map.setLevel(9);     // ← 이게 들어오면 위 이동 애니메이션이 취소됨
```
`panTo`는 애니메이션이라, 바로 뒤에 `setLevel`을 호출하면 **이동이 취소되고 중심이 이전 위치에 그대로 남는다.** 확대만 바뀜.
라이브 실측: 인천 연수구(37.423,126.644) 선택 후 5초 대기해도 중심이 강남(37.502,127.046) 고정, 레벨만 9→7 변경.
강남구가 "처음엔 정상"이었던 건 지도가 이미 그 근처라 이동이 필요 없었기 때문.

**수정:** 공용 헬퍼 도입 — **setLevel 먼저 → setCenter(즉시 이동, 취소 불가)**
```js
function moveMapTo(lat, lng, level){
  if(!map || !lat || !lng) return;          // 좌표 0/없으면 지도 안 건드림
  if(level) map.setLevel(level);
  map.setCenter(new kakao.maps.LatLng(lat, lng));
}
```
적용 5곳: `selectSido` / `selectSigungu` / `selectEmd` / `openComplexByCode` / 사이드박스 검색.
⚠️ **앞으로 지역 이동에 `panTo`+`setLevel` 조합 쓰지 말 것. `moveMapTo()` 사용.**

### 2. 뒤로가기 시 지도가 위경도 0,0으로 날아가던 잠재버그 (수정 완료)
`backToSigungu()`가 `selectSido(code, name, 0, 0)`으로 호출 → 아프리카 앞바다로 이동.
→ `moveMapTo()`가 좌표 falsy면 지도를 안 건드리도록 가드.

### 3. 사이드박스 전국데이터 폴백 제거 (수정 완료)
`selectSido`/`selectSigungu`가 `allAptData.length ? allAptData : MAP_DATA` 폴백을 쓰고 있었음.
`MAP_DATA`는 **지금 지도에 로드된 시군구만** 담고 있어서, 전국 좌표 파일(12.3MB)이 아직 안 왔을 때
엉뚱한 지역 목록이나 "데이터가 없습니다"가 뜸(느린 회선·첫 방문에서 재현).
→ 폴백 삭제. `rsDataReady()` false면 **"전국 단지 정보를 불러오는 중입니다…"** 표시,
   `loadAllAptData()` 완료 시 `rsRefreshAfterLoad()`가 열려있던 단계를 자동 재렌더.
→ 시군구 묶음 키를 이름(`d.sigungu`)에서 **코드(`d.sigunguCode`)** 로 변경(동명 시군구 방지).

### 4. 🔴 git "A lock file already exists" 반복 오류 — 근본원인 규명 + 해결

**원인:** Cowork VM은 기본적으로 **파일 삭제가 금지**(rm → Operation not permitted).
git은 어떤 명령이든(심지어 `git status` 같은 읽기 명령도) `.git/index.lock`을 만들었다 지우는데,
**지우질 못해서 매번 잔류** → GitHub Desktop 커밋이 "A lock file already exists"로 실패.

**해결:** `device_request_delete_permission`으로 폴더 삭제 권한을 받으면 정리 가능.
2026-09-05 세션에서 권한 받아 `.git/*.lock` 전량 + `_to_delete/`(잔류 20개) 삭제 완료.

**다음 인수자 지침:**
- 이 오류가 나면 → 삭제 권한 요청 후 `find .git -name "*.lock" -delete`
- 권한이 없으면 → `mv .git/index.lock _to_delete/` 로 치우고, **그 뒤 git 명령을 절대 쓰지 말 것**(다시 생김)
- 커밋·푸시는 여전히 사용자가 GitHub Desktop으로.

### 6. 🆕 사무소 등록 툴 + 소개/문의 페이지 (2026-09-05)

> 사용자 요구: "내 사무실 정보를 니가 코드에 그냥 박으면 안 되고, **사무실 추가하는 툴**을 만들어서 내가 쓰게 해줘."

**구조:** Supabase `offices` 테이블 ← 관리자 화면에서 입력 → `/about`, `/contact` 가 서버렌더로 읽음.

| 파일 | 내용 |
|---|---|
| `scripts/build_offices.sql` | `offices` 테이블 + `admin_secret` + RPC 4종. **Supabase SQL Editor에서 1회 실행 필요** |
| `admin-vkz/hub.html` | [사무소 관리] 탭 신설 — 추가/수정/삭제/대표지정. 로그인도 DB 검증으로 교체 |
| `api/aptpage.js` | `fetchOffice()` / `aboutPage()` / `contactPage()` + `?info=about\|contact` 라우팅 |
| `vercel.json` | `/about`, `/contact` rewrite 추가 |
| `sitemap-main.xml`, `api/indexnow.js` | `/about`, `/contact` 색인 대상 추가 |
| `index.html`, aptpage 푸터 | 소개·문의 링크 노출 (애드센스는 이 링크를 찾아야 함) |

**🔐 관리자 비밀번호 — 코드에서 제거됨 (중요)**
- 이전: `admin-vkz/hub.html` 에 `const ADMIN_PW = 'agugu9427'` 평문. **공개 레포라 누구나 열람 가능한 상태였음.**
- 현재: Supabase `admin_secret` 테이블에 **bcrypt 해시**로 저장. 로그인은 `check_admin_pw()` RPC가 서버에서 검증.
- 최초 설정: `/admin-vkz/hub` 로그인 화면의 "비밀번호 처음 설정하기" → 새 비밀번호 입력(기존은 비움).
- ⚠️ **`agugu9427` 은 이미 공개 노출된 값이므로 재사용 금지.**

**권한 모델 (RLS)**
- `offices` — RLS **ON**, anon `select` 정책만 있음. insert/update/delete 정책 없음 → 브라우저에서 직접 못 씀.
- `admin_secret` — RLS **ON**, 정책 없음 → 브라우저에서 아예 접근 불가.
- 쓰기는 `save_office(pw, jsonb)` / `delete_office(pw, id)` (SECURITY DEFINER, 비번 검증) 로만.
- ⚠️ 이건 **새 테이블에만 RLS를 켠 것**. 기존 `apt_*` 테이블은 여전히 RLS OFF여야 지도가 동작함 — SQL Editor에서 항상 **"Run without RLS"**.

**⚠️ Supabase 함정 (2026-09-05 실제로 걸림): `function crypt(text,text) does not exist` (42883)**
Supabase는 확장(pgcrypto)을 `public` 이 아니라 **`extensions` 스키마**에 설치한다.
`security definer` 함수에 `set search_path = public` 만 쓰면 `crypt()`/`gen_salt()` 를 못 찾는다.
→ 반드시 **`set search_path = public, extensions`**. 앞으로 pgcrypto·uuid 등 확장 쓰는 함수 만들 때 동일.

**✅ 2026-09-05 SQL 실행 완료 + 권한 실측 검증 (라이브 anon 키로 8종)**
| 검사 | 결과 |
|---|---|
| offices 공개 읽기 | 200 ✅ |
| offices 브라우저 직접 쓰기 | **401 차단** ✅ |
| admin_secret 읽기 | 빈 결과(못 읽음) ✅ |
| audit_log 직접 읽기 | 빈 결과(못 읽음) ✅ |
| 틀린 비번 → check_admin_pw | false ✅ |
| 틀린 비번 → save_office | 거부 ✅ |
| 틀린 비번 → read_audit_log | 빈 결과 ✅ |
| 8자 미만 비번 | 거부 ✅ |

`/about`, `/contact` 라이브 확인(200, 사무소 미등록 안내 정상 표시).
⬜ **남은 것: 사용자가 비밀번호 설정 + 사무소 등록** (비밀번호는 사용자만 정할 수 있음).

**동작 방식:** `is_primary=true` 인 사무소 1건이 `/about`·`/contact` 에 노출. 대표 지정이 없으면 가장 먼저 등록된 사무소를 씀. 등록이 하나도 없으면 "아직 등록되지 않았습니다" 안내만 뜨고 페이지는 정상 동작.

**사용자 사무소 정보 (2026-09-05 사용자 제공, 아직 미등록):**
상호 `(힐스자이단지내)구해줘부동산중개` / 대표 `배진원` / 사업자번호 `592-36-01680` /
주소 `충남 아산시 배방읍 동방로 169, 근린생활시설 106호` / 전화 `041-549-0119` / 이메일 `tandange@gmail.com`
⬜ **중개사무소 등록번호는 아직 안 받음** — 관리자 화면에서 사용자가 직접 입력.

### 7. 🔮 계정 시스템 (사용자 요구, 나중에 구현)

> 사용자 2026-09-05: "나중에는 비밀번호가 아니라 **관리자 아이디+비번** 눌러서 들어가고,
> 광고대행사(부동산뱅크 서브 같은 곳)·마케팅회사에서 손님을 땡겨오니까
> 그런 **팀장·직원들이 쓰는 아이디·비번을 만들어주는** 구조."

**즉, 공유 비밀번호 1개 → 사람마다 계정 + 권한 등급.**

지금(v22)은 `admin_secret` 1행짜리 공유 비밀번호. 이건 1인 운영용 임시 구조이고,
아래로 갈아타는 것을 전제로 만들었다 — `offices` 테이블이 이미 있으므로 계정이 사무소를 참조하면 된다.

**설계 방향 (미구현)**
- **Supabase Auth 사용 권장.** 비밀번호 해싱·세션·재설정·잠금이 다 들어있음. 직접 만들지 말 것.
  - 아이디(이메일 아님) 로그인을 원하면 내부적으로 `{아이디}@a99.local` 형태로 매핑.
- `accounts` (= auth.users 확장 프로필): `user_id` / `office_id`(FK→offices) / `agency`(소속 대행사) / `role` / `display_name` / `phone` / `active`
- **role 등급 예시**: `master`(사장, 전체) / `manager`(팀장, 자기 사무소·팀 전체) / `staff`(직원, 본인 것만) / `partner`(대행사, 자기가 넣은 매물만)
- **RLS가 이때부터 진짜 필요해짐.** 매물·손님 테이블에 "본인 소속만 보이게" 정책을 건다.
  ⚠️ 단, 기존 `apt_*` 지도 데이터 테이블은 계속 RLS OFF 유지(공개 데이터, 켜면 지도 깨짐).
- **활동 로그** 필수 — 누가 언제 무엇을 고쳤는지. 여러 명이 쓰면 분쟁이 반드시 생김.
- 개인정보(손님 연락처)를 다루게 되면 **접근 로그 + 최소권한**이 법적 요구사항이 됨(개인정보보호법).

**전환 시 할 일**: `check_admin_pw` / `set_admin_pw` 제거 → Auth 세션으로 교체, `save_office`·`delete_office` 의 `p_pw` 인자를 `auth.uid()` 기반 권한 검사로 교체.

### 8. 📒 로그 2종 — 감사 로그(구현됨) / 소개 담당자 추적(미구현·선설계 필요)

> 사용자 2026-09-05: "로그같은것도 남으면 좋을텐데 **누가 누구를 설정했고 누구 담당자가 소개해줬는지**"

**두 개는 성격이 완전히 다르다. 섞지 말 것.**

#### (A) 감사 로그 — ✅ v22에서 구현
- `audit_log` 테이블(`scripts/build_offices.sql`). RLS ON, 정책 없음 → 브라우저 직접 접근 불가.
- 조회는 `read_audit_log(pw, limit)` RPC로만. 관리자 [사무소 관리] 탭 하단에 표시.
- 지금 기록되는 것: `office.create` / `office.update` / `office.delete` / `admin.pw`
- `actor` 컬럼은 현재 `'관리자'` 고정. **계정 시스템 붙으면 여기에 아이디만 넣으면 됨** — 그래서 미리 컬럼을 뺐다.
- 확장 시: 매물 등록·수정, 손님 정보 열람도 같은 테이블에 `action` 만 늘려서 기록.

#### (B) 소개 담당자 추적 (attribution) — ⬜ 미구현, **설계 먼저**
수수료 정산의 근거가 되는 데이터. **놓치면 나중에 복구가 불가능하다** — 손님이 처음 들어온 순간에 기록해야 함.

**붙일 지점: 브리핑 짧은링크 `/b/{id}`.** 중개사·대행사가 손님에게 보내는 링크가 이미 이것이므로,
여기에 담당자 코드를 실어 보내면 첫 접촉이 그대로 기록된다.

설계안:
- `/b/{id}?ref={담당자코드}` 형태. 대행사·팀장·직원마다 고유 코드 발급.
- 첫 방문 시 `ref` 를 **쿠키/localStorage 에 보존**(30~90일). 이후 문의·전화 클릭 때 같이 전송.
  → 손님이 링크를 받고 며칠 뒤 연락해도 담당자가 유지됨 (**first-touch 귀속**).
- `leads` 테이블: `at` / `ref_code` / `office_id` / `brief_id` / `apt_code` / `채널`(링크·전화·카톡) / `연락처` / `메모` / `status`
- 정산 화면: 기간별 `ref_code` 집계 = 누가 몇 건 데려왔나.

⚠️ **법적 주의**: 손님 연락처를 저장하는 순간 개인정보 처리자가 된다.
- 수집 전 **동의 필수**(수집항목·목적·보유기간 고지) — hub.html 중개사 가입 폼에 이미 만들어둔 동의 UI를 재사용.
- 개인정보처리방침(`/privacy`) 에 수집 항목·보유기간 반영해야 함.
- 접근 로그 + 최소권한: 직원은 본인이 데려온 손님만 보이게 (RLS).

### 5. 다음 작업 (사용자와 합의, 2026-09-05)
| 순위 | 작업 | 상태 |
|---|---|---|
| 1 | **소개·문의 페이지** — 사무소 등록 툴로 구현 완료(위 6번). 남은 건 ①SQL 실행 ②관리자 화면에서 사무소 등록 | ✅ 코드완료 / ⬜ SQL+등록 |
| 2 | **필터 확장** — 세대수·입주년차·전세가율·갭·용적률·건폐율 (데이터 기보유, 칩만 연결) | 🔄 진행중 |
| 3 | **랭킹 종류 추가** — 거래량·거래회전율 등. SEO URL 증가 | 🔄 진행중 |
| 4 | bbox 서버쿼리 전환 → coords 12.3MB 로딩 제거 (큰 공사, 하루+) | ⬜ |
| 5 | 차트: 매매+전세 이중선 / 기간 슬라이더 / 단지 VS 비교 | ⬜ |
| 6 | 용적률 데이터 수집 — `apartments.far` 21,727 중 1,574개만 채워짐 | ⬜ |
| - | 애드센스 심사 결과 대기 / IndexNow 수동 제출 1회 | ⬜ |
| 7 | **계정 시스템** — 아이디+비번, 팀장/직원/대행사 권한 (위 7번) | ⬜ 나중에 |
| 8 | **감사 로그** (위 8-A) | ✅ 2026-09-05 |
| 9 | **소개 담당자 추적 / leads** (위 8-B) — 계정 시스템과 함께 | ⬜ 선설계 완료 |

---

## 🆕 v16 (2026-09-02) — 아실 벤치마킹 실측 분석 + 지도 고도화 로드맵

> 브라우저로 asil.kr 직접 열어 Network/DOM 실측. **코드 복제 아님 — 아키텍처 패턴만 기록.**
> ⚠️ 아실 CSS 클래스명(pin_nmbr, bg21 등)·마크업 그대로 옮기지 말 것. 애드센스 심사중이라 리스크 대비 실익 없음. 구현은 a99 스타일로 새로 작성.

### A. 아실 구조 (실측)
- **스택**: JSP 서버렌더 + jQuery 1.7/3.3 + **네이버지도**. SPA 아님, 프레임워크 없음.
- **핵심**: 지도 API가 JSON이 아니라 **실행 가능한 JS 문자열**을 반환.
  `removeAllMap(); displayLocationText(...); addCustomOverlay(lat,lng,0,1,99,'<div id="apt..." onclick="clickApt(...)">...</div>'); markerArray.push(_marker);`
  → 핀 HTML·색상·라벨·클릭핸들러를 **서버가 전부 생성**. 클라 렌더 로직 0줄. 필터 아무리 걸어도 안 느림.
- **줌 3단계 bbox 쿼리**: zoom11 `sido_appmap.jsp`(시도집계) / zoom13 `dong_appmap.jsp`(읍면동집계) / zoom14+ `apt_appmap.jsp`(단지핀) / 별도 `aptcount.jsp`(필터 통과 **개수만** → 필터UI 실시간 표시)
- 파라미터: `s_lat/s_lng/e_lat/e_lng + zoom + 필터13종 min/max`. **coords 파일을 프론트에 안 내려보냄** ← a99 coords 13.9MB 로딩 문제의 해답
- **핀 표시모드 12종** `setPinMode(n)`: 1세대수 2입주시기 3평당가격 4매물가격 5매매전세갭 6전세가율 7거래회전율 8대지지분 9실거래가 10용적률 11대지지분용적률 12건폐율
- **필터 13종**: m2(평형) household movein price py_price m_price j_price gap j_rate deal_rate land_area floor_area building_area
- **핀 구조**: `짧은이름 + 대표평형 + 가격` 2단 (예: 창원중동유니시티1단지 → **유니시티1** / 35평 9.8억). 미입주 단지는 가격 대신 세대수.
- **오버레이 API**: `data_traffic`(교통망, 연도슬라이더 2021~2027) / `data_redevelop`(재건축·재개발) / `data_education`(학군·학원수)
- **경매 핀**: 지도에 경매물건 직접 표시(오피스텔·상가·사무실·숙박·임야·답·단독·근생·지산) + 감정가 대비 하락률(↓66%)·유찰횟수
- **단지상세**: 매매/전세/월세 토글 + 평형선택 + **매매·전세 이중선 차트** + 기간슬라이더(06.1~26.9, '최근3년' 버튼) + **VS 단지비교** + 동/호 선택 → '집 내놓기'
- **홈 부동산스터디 20여종**: 최근하락·최고가·최고상승·가격변동·여러단지비교·매물증감·많이산단지·거래량·갭투자·매수심리·공급물량·미분양·분양가비교 ← **아실의 실제 해자, 검색유입 핵심**

### B. a99가 이미 이긴 것 (유지·강화)
관리사무소 전화번호 전국(아실 없음) / 관리비 K-apt(아실 없음) / PRO 매물안내서·브리핑 짧은링크 / **단지별 색인 URL `/apt/{code}` 21,727개** — 아실은 전부 `javascript:setMenu(...)`라 단지 URL 자체가 없음 → **SEO 구조는 a99 우위** / 분양핀+청약경쟁률

### C. ⚠️ 대표평형 — 오진 정정 (2026-09-02 재검증)

> **앞선 v16 초안에서 "배방삼정그린코아 대표평형이 버그"라고 적었으나 오진이었음. 아래가 정정된 내용.**

- **배방삼정그린코아(A33672927)는 정상**. coords `areaBreak` = `{u60:2156, u85:0, u135:0, o135:0}` → **전 세대가 60㎡ 미만인 소형 단지**. useArea 86,215㎡ ÷ 2,156세대 = 세대당 전용 40㎡. 대표 38㎡·6,500만원·전세가율 109% 모두 소형 단지에선 정상 범위.
- 오진 원인: 세대수(2,156)만 보고 "대단지=84㎡ 대표" 라고 단정. **평형 구성(areaBreak)을 안 봄.**

**실제로 확인된 문제 2건:**

1. **역전세 갭이 화면에서 사라짐 (진짜 버그, 수정 완료)** — `api/aptpage.js` `wonMan()`이 `man<=0`이면 `'-'` 반환. 배방삼정그린코아 갭 = 6,500 − 7,100 = **−600만원(역전세)**인데 화면엔 `-`. 역전세는 오히려 노출해야 할 정보. → 음수 부호 표기 + 라벨 `매매·전세 갭 (역전세)` 로 수정.
2. **혼합단지 소수평형 쏠림 (범위 좁음)** — 아산 혼합단지 11곳 라이브 실측:
   - ✕ 아산한라비발디스마트밸리(A10022824): 세대수 u60 244(24%) / u85 754(**76%**) 인데 **대표 55㎡** — 소수 밴드가 대표
   - △ 아산Y-CITY(A33672926): u60 504 / u85 552 / u135 413 (고르게 분산) → 대표 60㎡, 경계선
   - ✓ 나머지 9곳 정상(85㎡)
   → **11곳 중 1~2곳.** 전면 재설계 불필요, **밴드 가드 1개**로 해결: 대표평형이 세대수 소수 밴드면 최다 밴드로 올림.
   → `apt_area_break` 테이블(coords areaBreak에서 생성) + `build_metrics.sql` 가드 추가.

⚠️ **결론: 0번은 선행 필수 대형작업이 아님. 1·2번 진행을 막지 않음.**

### E. 2026-09-02 구현 메모

- **핀 지표 로딩**: `refreshPinMetrics()`가 지도 `idle`마다 뷰포트 bbox로 `apt_metrics` 조회 → `MPIN`(Map) 캐시. 줌 레벨 6 이상(핀 미표시)에선 스킵. `MPIN`이 아직 null이면 기존 세대수 표시 유지(깜빡임 방지).
- **⚠️ SQL 실행 전에도 안 깨지게 폴백 넣음**: `turn_rate`/`far`/`bcr` 컬럼이 없으면 select가 400 → `PIN_SEL_BASE`(기본 7컬럼)로 1회 자동 폴백. 즉 **index.html을 먼저 푸시해도 안전**하고, SQL 실행 후 거래회전율·용적률·건폐율 모드가 자동으로 살아남.
- **이름 축약** `shortName(name, sigungu, emd)`: 시군구·읍면동 접두 제거 → `아파트|APT|맨션` 접미 제거 → 8자 말줄임. 예) 아산배방우방아이유쉘2단지아파트 → 우방아이유쉘2
- **레포 정리**: `_patch_index.py`(1회용 패치)와 `index.html.bak-*`(자동 백업)은 `.gitignore`에 추가함. 확인 후 지우면 됨.

### D. 작업 순서 (합의)
| # | 작업 | 상태 |
|---|------|------|
| 0-a | 역전세 갭 표시 수정 (`api/aptpage.js` wonMan 음수 처리 + '역전세' 라벨) | ✅ 2026-09-02 |
| 0-b | 대표평형 밴드 가드 — `scripts/build_area_break.js` 신규(→ `build_area_break.sql` 20,693단지 생성 완료) + `build_metrics.sql`에 밴드 가드·`turn_rate`·`far`/`bcr` 추가 | ✅ 코드완료 / ⬜ SQL실행 |
| 0-c | `build_pyeong.sql`/`build_metrics.sql` 재실행 (재워밍 반영, 계속 대기중) | ⬜ 사용자 |
| 1 | **핀 표시 모드 드롭다운** 13종(명칭+12) — `PIN_MODES`/`PIN_MODE_ORDER`, `refreshPinMetrics()`가 apt_metrics를 뷰포트 bbox로 읽어 `MPIN` 캐시. 매물가격·대지지분은 `soon` 회색 처리 | ✅ 2026-09-02 |
| 2 | **핀 라벨 개편** — 세로 2단 유지. `shortName()` 이름축약 + 아랫칸을 모드별 값(+대표평형 칩)으로 교체, 값 없으면 회색 '-' | ✅ 2026-09-02 |
| 2.5 | **지도 컨트롤 개편** — `setMapBase()`(지도/스카이뷰 택1) + `toggleRoadview()`/`toggleDistrict()`(각각 on/off) + `mtSync()`. 베이스 전환해도 오버레이 유지(예전엔 강제로 꺼졌음). `setMapType()`은 호환 shim으로 남김 | ✅ 2026-09-02 |
| 3 | **거래회전율** `turn_rate` = deal_cnt/units*100 (18개월) — build_metrics.sql에 추가, 핀 모드에도 연결 | ✅ 코드완료 / ⬜ SQL실행 |
| 4 | **필터 확장** — 세대수·입주년차·전세가율·갭·용적률·건폐율 (평형·시세는 기보유) | ⬜ |
| 5 | bbox 서버쿼리 전환 + 줌 시도단계 추가 → coords 13.9MB 로딩 제거 | ⬜ |
| 6 | 차트: 매매+전세 이중선 / 기간 슬라이더 / 단지 VS 비교 | ⬜ |
| 7 | **랭킹 페이지 5종** — `/rank` + kind 5 × 시도 17 = 90 URL. `api/aptpage.js`에 병합(⚠️ Vercel 함수 12개 한도 꽉 참 → `api/rank.js` 신규 불가) | ✅ 2026-09-03 |
| 8 | (별건) 매물DB / 경매(온비드) / 재개발 / 교통망 / 학군 | ⬜ |

---

## 🆕 v14 변경사항 (2026-08-31 세션) — 실거래/관리비/평형·시세/브리핑 시스템 + SEO 실거래삽입 + 애드센스 신청

> ⚠️ 이 문서가 07-17에서 멈춰있어서, 그 이후 구축된 주요 시스템을 여기 한 번에 정리함.

### A. 실거래 캐시 + 전국 재워밍 (핵심)
- **`apt_transactions`** 테이블 = 단지(kapt_code)×월(ym) 단위 실거래 캐시(buy/jeonse/monthly jsonb). `/api/trade`가 여기서 읽고, 없으면 국토부 라이브 조회 후 채움.
- **`api/warm.js`** = 시군구(lawd)+월 단위로 국토부 받아 그 시군구 모든 단지에 매칭해 캐시. 외부 크론이 `/api/warm?secret=3c4fc034da972bac20f024ef003bf1ec` 를 몇 분마다 호출. 상태확인 `?status=1`. **재워밍 100% 완료됨.**
- **매칭 로직(trade.js `aptMatch` + warm.js `makeMatcher` — 두 곳 동기화 필수)** 이번 세션 개선:
  - 시행사 접두사 제거: `(주)/㈜/(유)/주식회사 XXX ` 떼고 매칭 (호려울마을10단지 등 6개)
  - 신도시 정준식별자: `○○마을N단지` 추출해 브랜드 위치 달라도 매칭 (세종·은평뉴타운 29개)
  - 화성類(구코드 41591 ↔ 국토부 41597): warm은 4자리 prefix로 형제코드 0~9 전부 조회, trade는 sibling-code 폴백. **정상 캐시 확인.**
  - 자가치유: 매칭개선 前 '0건 박제' 캐시는 `fetched_at < 2026-08-04T12Z`면 1회 재조회.
- 전수 census(이번 세션): 큰단지(≥1000세대) 24개 0건 없음, 전크기 40개 중 2개만 빔(둘 다 세종 마을단지→위 fix로 해결). **잔여 gap은 극소수.**

### B. 관리비 (온디맨드 캐시)
- **`apt_mgmtcost`** 테이블 + `/api/apt?type=mgmtcost&code=&units=` (12함수 제한 때문에 apt.js에 병합). K-apt 관리비 API를 첫 조회 시 받아 캐시(`cached:true`). 단지페이지·브리핑·사이드바에서 사용.

### C. 평형·시세 필터 (서버측, 아실 방식)
- **`apt_pyeong`**(9구간 버킷) + **`apt_pyeong_blob`**(프론트 로딩용) + **`apt_metrics`**(단지별 대표 매매/평당/전세/전세가율/갭/거래건수). 지도 뷰포트+필터가 `apt_metrics`를 bbox 쿼리.
- 생성 SQL: `scripts/build_pyeong.sql`, `scripts/build_metrics.sql` — **실거래 갱신 후 재실행하면 최신화**(idempotent, on conflict update). ⚠️ **재워밍으로 새로 채워진 데이터 반영하려면 이 2개 SQL 재실행 권장**(apt_pyeong 현재 18,350/21,718 커버).

### D. 매물 브리핑 (중개사 → 손님 공유 링크)
- **`briefs`** 테이블(id text, data jsonb). 지도 PRO탭 매물안내서에서 여러 매물 담아 **짧은링크 `a99.co.kr/b/{id}`** 생성(`doShareLink`). `/b/:id`→`api/apt` `handleBrief`가 **카톡 OG 미리보기**(og:title/description/image=og-brief.png) 렌더 후 `brief.html?id=`로 리다이렉트. 손님뷰 `brief.html`.
- **분양·미입주 직접입력 개선(이번 세션)**: 검색 결과없음 시 "직접입력으로 담기" CTA, 토글 가시성 강화, **직접입력 열리면 이전 선택단지 무시(우선) + code='' 처리**(관리비·실거래 링크 없이 이름·가격만). 손님링크 code없어도 정상 렌더 확인.
- 광고 배너 애드핏 `DAN-KpmCmBSL2hBTN2xb`(브리핑 상단).

### E. SEO — 단지페이지 실거래·시세 서버렌더 삽입 (색인 강화, 이번 세션 핵심)
- `api/aptpage.js`가 렌더 시 **apt_metrics 읽어 실거래가·평당가·전세가·전세가율·갭·거래건수를 HTML에 직접 박음** + FAQ("○○ 실거래가 얼마?") + meta desc에 "최근 실거래 X억". 이전엔 전화번호+템플릿만이라 구글이 "빈약 페이지"로 색인 미룸 → 이걸로 알맹이 추가. **배포됨(라이브 확인).**
- **IndexNow 주간 크론**(`vercel.json` crons에 `/api/indexnow?secret=…` 월04시) — 네이버·빙 자동 제출. `api/indexnow.js` + 키파일 `3c4fc034….txt`.
- 단지제보 FAB(왼쪽하단) 제거 → 헤더 + 모바일☰메뉴로 이동.

### F. 애드센스 신청 (진행중, 이번 세션)
- 게시자 **`ca-pub-3915435017395988`**. index.html+aptpage(단지/지역) `<head>`에 인증 메타+adsbygoogle 스크립트, 루트 **`ads.txt`**(`google.com, pub-3915435017395988, DIRECT, f08c47fec0942fa0`). **사용자가 검토요청 제출 완료** → 심사 대기(며칠~2주). ads.txt "찾을수없음"은 구글 크롤 지연(정상).
- GDPR(유럽규정) 동의 메시지: 사이트 연결+개인정보처리방침 URL까지 했으나 **로고 필수라 게시 보류**(초안 저장). 미국주 메시지 미생성. **승인·수익엔 무관(EU 방문자용, 한국사용자 안뜸)** — 나중에 로고 올려 게시하면 됨.

### G. 애드핏 수익 진단 (참고)
- "예전 1,000~2,000원 → 지금 7원" 원인 = **무효클릭 회수.** 옛날 고수익일 CTR 2~3.5%(정상 디스플레이 0.1~0.5%의 5~30배)= 본인/비정상 클릭 → 애드핏이 걸러냄. 현재 CTR~0%가 실제 baseline. **광고는 정상(Fill Rate 90~96%).** ⚠️ 본인 광고 클릭 금지(반복 시 계정 영구정지).
- notice/516(SDK 도메인 t1.daumcdn→t1.kakaocdn, 8/26중단) = a99는 이미 새 도메인이라 **무관, 수정불요**. (daumcdn은 주소검색 postcode API만, 광고와 별개)

### H. 색인 현황 (2026-08-31)
- **구글**: 색인 691 / 미색인 21,620 (~3%). 원인 = 신생사이트 크롤예산 배분 + (오늘 개선 전) 빈약페이지. E의 실거래삽입 재크롤로 개선 기대(몇 주). 가속법 = GSC URL 색인요청(단지별, 하루 10~20 한도).
- **네이버**: 서치어드바이저 크롤 정상(하루 500~640페이지 완료, 제한~0, 다운로드 300~500ms). 건강하게 수집 중.
- 냉정한 판단: 공공데이터 2만 페이지 양산은 검색엔진이 경계 → 전부 색인/상위노출은 어려움. 트래픽엔 차별화기능·유입경로가 더 중요할 수 있음(전략 재검토 여지).

### ⚠️ RLS 주의 (반복 이슈)
- 신규 Supabase 테이블은 생성 시 RLS 켜짐 → anon 쓰기 401/42501. **멀티스테이트먼트 SQL 안의 `alter table X disable row level security`는 안 먹힐 때 있음 → 반드시 단독 실행.** 영향받은 테이블: apt_mgmtcost, apt_pyeong(_blob), apt_metrics, briefs. (전부 현재 disable 완료)

---

## 🆕 v10 변경사항 (2026-07-13 세션) — 핀 애니메이션 제거, 실거래 공백 원인 확인

### 1. 아파트 핀 등장 애니메이션 제거
- 이전 세션에서 추가했던 `pinPop` CSS + `pin-pop` className + `animationDelay` 전부 제거
- 이유: 사용자가 "눈아프다 그냥 뜨게 해줘" → 핀이 애니메이션 없이 즉시 표시됨
- 수정 위치: index.html CSS(@keyframes pinPop/.pin-pop 삭제), addMarker(_delay 변수+className 제거), renderPresalePins(pin-pop className+animationDelay 제거)

### 2. 실거래가 특정 단지에서 24년 12월까지만 표시되는 현상 원인 확인
- 경주두산위브트레지움 등 일부 단지에서 차트가 24년 12월 이후 없음
- **결론: 코드 버그 아님 — 해당 단지에 실제 거래가 없는 것**
- TRADE_MONTHS는 2026년까지 정상 설정됨. CDN 없어서 배치 모드로 국토부 직접 조회하는데 빈 결과 반환 = 거래 없음

### 3. Claude Fable 5 접근 불가 (참고)
- 2026-06-12 미국 정부 수출통제 명령으로 Fable 5/Mythos 5 전 세계 차단됨
- 현재 Opus 4.8이 최고 모델. Claude Code /compact 로 컨텍스트 압축 가능

---

## 🆕 v9 변경사항 (2026-06-14 세션) — 사전적재 백필 0% 멈춤 원인 규명·수정

### 1. 🔴 백필 전면 정지 원인: 분양권 API 실패가 그 달 통째 폐기 (수정 완료)
- **증상**: guhaedo-data `progress.json`의 전국 250개 시군구 커서가 **전부 `202607`** (= 한 발도 못 나감). `trades/*.json` 0개 생성. → 프론트가 항상 라이브 배치 폴백 → 차트가 느리고 월별로 실시간 누적 렌더돼 "움직이는" 현상
- **원인**: `api/diag.js` op=district가 매매·전월세·분양권 3종을 호출하고 **하나라도 실패하면 `apiError:true`** 반환 → `build.mjs` fetchMonth가 throw → 그 달 폐기(done=0). 백필은 매번 현재월(202606)부터 시작하는데, 분양권(SilvTrade)이 **간헐 타임아웃**으로 그 첫 달을 실패시켜 전 시군구가 출발선에 묶임
- **확인**: 라이브 종로 202604 → buy/rent 정상 + `"pre":[],"apiError":true`. 영통 202503 → 전부 정상 (분양권 간헐 실패 = 타임아웃성)
- **수정**:
  - `api/diag.js`: 분양권을 **선택 데이터로 격리**. 매매·전월세만 성공하면 분양권 실패해도 캐싱+수집 진행. `apiError`는 매매/전월세 실패에만 설정. 분양권 1차 실패 시 15s로 1회 재시도. 응답에 `preError` 플래그 추가
  - `api/trade.js`(배치): 동일 철학 — 공유 `err`를 `errCore`(매매/전월세)/`errPre`(분양권)로 분리 → 분양권 실패가 월 캐싱을 막지 않음(백필 완료 전 라이브 속도 개선)
  - ⚠️ build.mjs 수정 불필요 (diag가 더 이상 분양권 단독 실패로 apiError를 안 보내므로 기존 `if(j.apiError) throw`가 올바르게 동작)

### 2. 실거래 2006~현재 전체 표시 — 화면 수정 불필요
- 프론트는 단지 클릭 시 이미 `max(2006, 준공-3년)~현재`를 기본 범위로 설정(index.html, 약 2044줄). 데이터만 채워지면 자동으로 전 기간 표시
- `build.mjs` `START_YM=200601`(2006.1 = 실거래 신고 의무화 시점)이 백필 목표. 위 수정으로 정상 진행 예정

### 3. ⚠️ 배포 + 모니터링 (사용자/다음 인수자 할 일)
- `api/diag.js`, `api/trade.js`를 **GitHub Desktop으로 커밋·푸시**해야 적용(Vercel 자동배포)
- 배포 후 guhaedo-data 레포 **Actions → update-data → Run workflow**(수동) 또는 다음 새벽 3시 자동 실행
- 정상 신호: `progress.json` 커서가 `202607`에서 **과거 월로 내려가기 시작**(예: 202512, 202412…). `trades/{단지코드}.json` 파일 생성 확인
- BUDGET 단위 점검 권장: `update.yml` 기본 `BUDGET=9000`을 build.mjs는 **프록시 콜 수**로 사용(프록시 1콜=국토부 3콜). 의도가 "국토부 9000콜/일"이면 레포 Variables의 BUDGET을 **3000**으로 (현재는 API별 9000콜이라 한도 1만/일 이내일 수 있음 — 백필 1~2주 예상)
- 분양권 보라점이 계속 비면 data.go.kr "분양권전매 실거래가(RTMSDataSvcSilvTrade)" 한도/활용신청 점검

### 4. 🔴 신축 단지 "거래내역없음" = 분양권 지연으로 함수 시간초과 (수정 완료, 2차 푸시 필요)
- **증상**: 신축(준공 ±1년) 단지 차트가 "거래내역없음". 구축은 정상. (캐시 안 된 달이 섞인 요청에서 발생)
- **원인**: 신축은 분양권(pre) 호출이 켜지는데(wantPre=true), 분양권 API가 무응답으로 매달림 → `/api/trade` 함수가 Vercel 제한시간(약 10초) 초과 → 504/빈 응답 → 프론트 `if(!d)break` → "거래내역없음". (구축은 wantPre=false라 분양권 호출 안 해서 멀쩡했음)
- ⚠️ v9에서 넣었던 diag.js 분양권 15초 재시도도 같은 함정(diag도 Vercel 함수 → 백필이 diag 504 받으면 다시 멈춤) → **fail-fast로 철회**
- **수정**:
  - `api/trade.js`: 분양권 타임아웃 **3초 fail-fast**(매매/전월세 5초), 배치 동시처리 6→12(미보유 월 1웨이브 처리) → 함수 총시간 < 10초 보장
  - `api/diag.js`: 분양권 재시도 제거 → **4초 fail-fast**, 매매/전월세 7초(캐시조회 2.5초 + 7초 < 10초)
- 참고: 신축의 '매매'는 대부분 분양권 거래라, 분양권 API가 죽어 있으면 timeout을 고쳐도 매매 점은 비어 보일 수 있음(전세/월세는 표시됨). 분양권 점은 data.go.kr API 정상화가 별도로 필요

### 5. 🔴 일부 단지 "거래내역없음" = 오염된 빈 캐시 (캐시 버전컷 갱신)
- **증상**: 경주두산위브트레지움(A10024512, 용강동 1813) 등 일부 단지가 매매/전세/월세 전부 "거래내역없음"
- **확인**: 국토부 원본엔 거래 존재(두산위브트레지움/용강동/1813/2020, 42,800만원 등), 매칭 로직도 정상(로컬에서 addrMatch=true) — 그런데 apt_transactions에 **빈 값 행이 박제**돼 `fromCache:true`로 반환됨
- **원인**: 분양권 지연/타임아웃이 만연하던 기간에 빈 결과가 캐시에 저장됨. 2026-06-11 버전컷 이후 생성된 행이라 자동 무효화가 안 됨
- **수정**: trade.js 캐시 버전컷 2곳 `2026-06-11T08:10:00Z` → **`2026-06-14T07:00:00Z`**로 상향 → 기존 캐시 전부 무효화 → 타임아웃 고친 코드로 재조회·재캐싱
- ⚠️ 매칭/수집 로직 바꿀 때마다 이 버전컷을 현재 UTC로 올리는 게 오염 캐시 청소법 (getCached/getCachedBatch 2곳)
- 참고: 이 단지는 coords에 준공년도 None → wantPre가 모든 달 true → 분양권 호출 최다 → 타임아웃 직격탄. check_missing_meta.js/fill_meta.js로 누락 준공년도 채우면 더 견고

---

## 🆕 v8 변경사항 (2026-06-11~13 세션)

### 1. 실거래 면적 필터 소수점 정밀 구분
- 기존 `Math.floor(x.a)` 그룹핑 → 84.5678 ≠ 84.7689가 같은 84로 묶이던 버그 수정
- 수정: 키를 `String(parseFloat(x.a))`로 저장, 비교 시 `Math.abs(x.a - parseFloat(filter)) < 0.0001`
- 면적 표기: 공급면적 API 데이터 없음 → 전용㎡만 표시 (전유부분 평수 표기 제거)
- A/B suffix 자동 부여 로직 제거 (단정짓지 않음)

### 2. .gitignore 생성
- `아구구_인수인계서.md`, `.env`, `.DS_Store` 등 커밋 제외 설정

### 3. admin/index.html — 중개사 담당단지 기능
- broker_apts 테이블 신규 (broker_id, apt_code, apt_name)
- 어드민 중개사 폼: 담당단지 검색+추가 UI
- index.html 아파트 사이드바: 전문중개사 카드 섹션 표시

### 4. master/index.html 전면 재작성 (5탭)
- 📊 현황 / 🔍 실거래 진단 / 🏢 건축물대장 / 🔧 수동 진단 / ⚙️ 설정
- 비번 localStorage `mst_pw` 기본값 **9999** (로그인 `mst_auth`=1 sessionStorage 유지)
- 시도 카드 → 시군구별 api/diag?op=district 1회 공유 → 의심단지 [복사] 워크플로
- 건축물대장 누락 진단, op=fixmeta 수동 수정

### 5. api/diag.js 신규 생성
- op=district: 국토부 매매/전월세/분양권 3종 동시 호출 + Supabase `district_cache` 캐싱
- op=metalist: apartments 테이블 메타 일괄 조회
- op=fixmeta: apartments 테이블 수동 수정 PATCH
- ⚠️ **`supabase_district_cache.sql` Supabase에서 실행 필요** (아직 미실행 시)

### 6. 분양 핀 설계 (⚠️ 건드리지 말 것)
핵심: `presaleDup()` + `rebuildPresaleActive()` 시스템 — Opus 설계, 반드시 유지
- `presaleDup(d)`: 활성 분양 핀과 40m 이내 OR 120m+이름 유사 → 일반 핀 억제
- `rebuildPresaleActive()`: presaleStatus()가 non-null인 분양 단지 인덱스 빌드
- `presaleStatus()`: 분양예정/분양중/D-n개월(12개월 이상 "1년 n개월") → `null` 반환 시 분양 핀 제거, coords 일반 핀 자동 표시
- `renderPresalePins()`: ps-pin 무지개 CSS, level ≤ 5에서만 표시
- addMarker에서 bunyang 타입은 완전 스킵 (`presaleDup`으로 apt/officetel 선택적 억제)

### 7. git 방식 확인
- 사용자는 **GitHub Desktop** 사용 (CMD git 아님)

---

## 🆕 v7 변경사항 (2026-06-10~11 세션) — 실거래 전면 개편

### 1. 실거래 매칭 로직 v4 ⚠️ 3곳에 동일 로직 존재 — 수정 시 반드시 3곳 동기화
위치: `api/trade.js`(aptMatch), `master/index.html`(matchItem), **guhaedo-data 레포** `build.mjs`(matchItem)
- 우선순위: ① 주소일치(법정동+지번본번, 부번은 양쪽 있을 때만) → 단지명 무시
  ② 이름 완전일치+법정동 일치 → 지번 무시 (K-apt 지번≠국토부 지번 흔함: 모지번/합필)
     · 동 불일치 시엔 본번+부번 완전일치일 때만 (법정동 개편 대응, 예: 전주 동산동→여의동)
  ③ 부분포함(정규화 5자+): 추가글자가 숫자없는 ≤4자(지역명/브랜드 접두접미: 전주·군산·한화 등)면 지번 무관 매칭, 숫자 포함(2차·3단지)이면 본번 일치 필수
  ④ 4자 이름(황등오투·동신진주 등)은 본번 완전일치 필수 (generic 보호: 휴먼시아·해송마을은 의도적으로 안 잡음)
- 정규화: 공백/괄호/특수문자 제거→대문자→`(아파트|APT|맨션|관리사무소|관리동)$` 제거×2
- 공통 필터: cdealType(계약해제) 제외, buildYear ±1년
- 분양권/입주권: RTMSDataSvcSilvTrade API → buy 배열에 병합, `pre:'분'|'입'` 플래그 (차트 보라점, 준공+1년 이후 월은 호출 스킵)

### 2. api/trade.js 배치 모드 (속도 개선 1단계)
- `?yms=202501,202502,...` → Supabase 캐시 일괄조회 → 전부 캐시면 외부 API 0회 즉시응답 (~0.5초)
- 미보유 월은 12개씩 서버가 페치 후 `missing` 배열 반환 → 프론트가 루프 백필 (최신부터 렌더)
- ⚠️ **캐시 버전컷 상수** `Date.parse('2026-06-11T08:10:00Z')` (getCached/getCachedBatch 2곳) — **매칭 로직 변경 시 이 시각을 현재 UTC로 갱신**해야 오염 캐시 무효화
- apt_transactions의 apt_name 컬럼 = `"단지명|법정동|지번"` 패킹 (캐시 히트 시 V4 스킵용)

### 3. 사전적재 시스템 (아실 방식, 속도 최종) — 이번 세션 핵심
- **레포: github.com/BILLIONARIE-CAN/guhaedo-data** (Public, SERVICE_KEY 시크릿 등록 완료, 2026-06-11 첫 실행 시작)
- GitHub Actions 매일 03시 KST 자동: 전 시군구 최근 3개월 갱신 + 과거 백필(2006년까지, progress.json의 cursor가 'done'이면 완료)
- 일 예산 9,000콜 → 전국 백필 2~3주 소요. 레포 Settings→Variables에 `BUDGET` 추가로 가속(운영계정 승인 시)
- push는 orphan 브랜치 force push = 히스토리 안 쌓임 (용량 = 항상 최신 스냅샷 크기)
- 프론트(index.html loadTradeChart): `TRADE_DATA_CDN`(jsDelivr) 정적 우선(~0.3초) → 최근 2개월 라이브 교체 → 404 시 배치 모드 폴백
- jsDelivr 캐시 12시간. 데이터 형식: trades/{단지코드}.json `{n,u,b:[[ym,일,가격만원,전용㎡,층,(1분양/2입주)]],j:[...],m:[[ym,일,보증,월세,㎡,층]]}`

### 4. 마스터 페이지 v2 재작성 (guhaedo.kr/master, 비번 localStorage mst_pw 기본 9999)
- 🔍 실거래 진단: 시도 카드 클릭→시군구별 실거래 API 1회 공유(api/diag.js op=district→Supabase district_cache 캐싱)→클라 매칭→❌의심단지 [복사]버튼→사용자가 Claude에게 붙여넣는 워크플로
  · 정상단지 30일 스킵(localStorage mstTradeDiag_v2), 의심단지는 /api/trade 단건 호출로 2차 재검증(가짜 의심 자동 제거)
- 🏢 건축물대장 진단: 누락(용적률/건폐율/승인일/난방/주차) 목록→[자동보완](api/apt detail 재조회)/[수정](op=fixmeta로 apartments PATCH)
- ⚠️ Supabase `district_cache` 테이블 필요 — 루트의 `supabase_district_cache.sql` 실행 여부 확인할 것
- 구 마스터의 apt_overrides 오버라이드 탭 삭제 (trade.js가 읽지 않던 死기능)

### 5. 기타 수정
- api/apt.js 표제부: numOfRows 1→100, 용적률>0 행 중 연면적 최대(주건축물) 선택 — 관리동이 첫 행이면 용적률 빈 값 나오던 버그 수정
- 지도: 아파트 핀 레벨5(250m)부터, 레벨6(500m)은 읍면동 클러스터 (마커 수백개 렉 방지)
- 차트 기본범위: max(2006, 준공-3년)~현재 (분양권 시기 포함). 준공-2년 클라 필터 제거
- 면적 드롭다운: 정수㎡ 그룹핑 + "29평A (84.97㎡)" 표기. 거래내역 테이블 분양권/입주권 보라 라벨
- trade-debug 라인: 조회조건 표시, 매칭 0건 시 국토부 등록명 후보 표시

### 5.5 분양 단지 핀 (청약홈)
- guhaedo-data 레포 `presale.mjs`: 청약홈 분양정보+경쟁률 API → 카카오 지오코딩(b_code로 lawd 획득) → presale.json. 워크플로에 스텝 포함(매일)
- ⚠️ **공공데이터포털에서 활용신청 2건 필요**: "한국부동산원_청약홈 분양정보 조회"(15098547), "청약홈 청약접수 경쟁률"(15098905) — 미승인 시 presale 수집만 스킵됨(실거래 수집은 정상)
- 프론트(index.html): PRESALE_URL CDN 로드 → 주황/빨강/보라 핀(분양예정/분양중/입주D-n, 레벨≤8 표시, 입주+10개월 후 자동 소멸) → 클릭 시 openAptDetail 재활용(code='DISC_'+lawd+'_'+단지명 → 분양권 실거래 차트 자동 연동) + presale-box 정보(공급세대·청약일정·경쟁률·문의처)
- 지오코딩 실패 단지는 Actions 로그에 "📍 지오코딩 실패"로 출력 — 추후 마스터에서 수동 보정 기능 고려

### 6. 미해결/다음 작업
- [ ] 진단 잔여 의심단지(4자 generic·복수 후보: 휴먼시아/해송마을/오투그란데류) 단지별 수동 매칭 지정 기능 — 사용자가 잔여 목록 주면 설계
- [ ] guhaedo-data 백필 진행 모니터링 (레포 Actions 로그, progress.json)
- [ ] 마스터 진단을 시도별 1회씩 돌리면 사전적재와 별개로 Supabase 캐시도 예열됨

---

## 📌 프로젝트 개요

- 사용자: 충남 아산 공인중개사. 브랜드 **아구구**
- 사이트: https://guhaedo.kr (GitHub 레포 `guhaedo` + Vercel 자동배포)
- 관리자: https://guhaedo.kr/admin (비번 **8888** → 변경 권장)
- 작업 파일: `C:\Users\essoz\GitHub\guhaedo` (Cowork 연결 폴더)
- 사용자는 개발자가 아님. cmd 명령어, 파일 위치 구체적으로 안내할 것
- git은 CMD에서 안됨 → **Git Bash** 사용 또는 전체 경로 사용
- ⚠️ **Claude 샌드박스에서 이 레포에 git 명령(fetch/status 등) 실행 금지** — `.git/index.lock`이 생기고 권한 문제로 못 지워서 사용자 커밋이 막힘 (발생 시 CMD에서 `del /f "C:\Users\essoz\GitHub\guhaedo\.git\index.lock"`). 코드 확인은 grep/Read로만

---

## 🔑 핵심 키/정보

| 항목 | 값 |
|------|-----|
| 공공데이터 API KEY | `8dfbbd6dc2fff98040507b95b9688bc24cbdfb35e253494d734a697d4658f1cf` |
| 카카오 REST KEY | `be82d140cac4386ee76d82cc16c65c3e` |
| 카카오맵 앱키 | `a7a67059ffadc0b7c6e4318639cdedeb` |
| 나이스(NEIS) API KEY | `980e346ab8c348ae85f1d484fb3e275c` |
| Supabase URL | `https://nqnbbccazjanjhktknyz.supabase.co` |
| Supabase anon key | (공개키 — 레포 내 보관) |

---

## 🔧 API 엔드포인트

| 용도 | URL | 주요 파라미터 |
|------|-----|--------------|
| K-apt 단지정보 V4 | `https://apis.data.go.kr/1613000/AptBasisInfoServiceV4/getAphusBassInfoV4` | serviceKey, kaptCode, _type=json |
| 건축HUB 건축물대장 표제부 | `https://apis.data.go.kr/1613000/BldRgstHubService/getBrTitleInfo` | serviceKey, sigunguCd, bjdongCd, bun, ji, numOfRows=1 |
| 건축HUB 총괄표제부 | `https://apis.data.go.kr/1613000/BldRgstHubService/getBrRecapTitleInfo` | ⚠️ 아파트 단지엔 totalCount=0 — 사용 불가 |
| 카카오 주소검색 | `https://dapi.kakao.com/v2/local/search/address.json` | query, Authorization: KakaoAK |
| 카카오 키워드검색 | `https://dapi.kakao.com/v2/local/search/keyword.json` | query, Authorization: KakaoAK |

**K-apt 주요 응답 필드:** kaptAddr, doroJuso, kaptBcompany(시공사), codeHallNm(현관구조), kaptdaCnt(세대수), kaptDongCnt(동수), codeHeatNm(난방), codeSaleNm(분양), kaptUsedate(준공), ktownFlrNo(최고층), bjdCode(법정동코드)

**API 일일한도:** K-apt 약 1만 건/일 (자정 초기화)

---

## 🗄️ Supabase 테이블

| 테이블 | 용도 | 상태 |
|--------|------|------|
| brokers | 중개사 (지도 핀) | ✅ 사용중 |
| broker_apts | 중개사 담당단지 (broker_id, apt_code, apt_name) | ✅ 생성됨 |
| businesses | 업체 (법무사/세무사/이사/청소/수리/기타) | ⬜ 생성 필요 |
| banners | 배너 광고 (읍면동 타겟팅) | ⬜ 생성 필요 |
| district_cache | 시군구 실거래 월별 캐시 (api/diag 공유) | ⚠️ SQL 실행 필요 (`supabase_district_cache.sql`) |
| apartments | 단지 메타 (far, bcr, use_date, heat_name, sale_name, total_park) | ✅ 사용중 |
| apt_transactions | 실거래 캐시 kapt_code×ym (buy/jeonse/monthly jsonb) — /api/trade·warm.js | ✅ 사용중 (RLS off) |
| warm_state | 재워밍 커서(id=1, cursor/calls_today/day) | ✅ 사용중 |
| apt_mgmtcost | 관리비 온디맨드 캐시 — /api/apt?type=mgmtcost | ✅ 사용중 (RLS off) |
| apt_metrics | 단지별 대표 시세지표(price_m/py_m/jeonse_m/j_rate/gap_m/deal_cnt) — 지도 필터·단지페이지 | ✅ 사용중 (RLS off) · `scripts/build_metrics.sql` 재실행으로 갱신 |
| apt_pyeong / apt_pyeong_blob | 단지별 9구간 평형버킷 — 평형 필터 | ✅ 사용중 (RLS off) · `scripts/build_pyeong.sql` 재실행으로 갱신 |
| briefs | 매물 브리핑 저장(id text, data jsonb) — 짧은링크 a99.co.kr/b/{id} | ✅ 사용중 (RLS off) |

**businesses 컬럼:** id(uuid,auto), name, category, phone, addr, lat(float8), lng(float8), photo, active(bool,true), expires_at(date), memo, created_at(timestamptz,now())

**banners 컬럼:** id(uuid,auto), business_name, category, phone, image, areas(jsonb), active(bool,true), expires_at(date), created_at(timestamptz,now())

---

## ✅ 완료된 작업

### 1. 아파트 데이터 (split_output/)
- 전국 **21,825개** 단지 (기존 21,308 + 누락 517개 추가)
- 기본: code, name, lat, lng, units, emd, sigunguCode, sido, sigungu, built, dongCnt, heat, sale, topFloor, addr
- 엑셀(20260605_단지_기본정보.xlsx) 보완 완료:
  - entrance(현관구조), builder(시공사), totalPark(총주차대수), units(세대수 0→실값), dongCnt, topFloor
  - 20,480건 갱신
- K-apt API 추가 수집 완료 (19,999+10,000건):
  - jibunAddr, builder, entrance, useArea, bjdCode, aptType, mgrType, areaBreak
- 누락 아파트 517개 카카오 지오코딩으로 추가
- 12개 끝내 실패 (신축 입주 전 / 너무 오래된 단지 → 카카오 미등록)

### 2. 사이드바 단지 상세 (index.html)
- MAP_DATA에 jibunAddr, builder, entrance, aptType, mgrType, totalPark 추가
- 세대수: units=0이면 hhldCnt fallback
- 총 주차대수: `d.totalPark.toLocaleString()+'대'` 표시
- 사이드바 `d.parking` → `d.totalPark` 수정

### 3. 매물안내서 정보카드
- 총주차(ci-park) 항목 추가
- 시공사(ci-builder), 현관구조(ci-entrance) 표시

### 4. 지도 (index.html)
- 핀 색상: 아파트=파랑 / 분양=노랑 / 오피스텔=보라 / 연립=청록
- 학교 핀 🏫, 업체 핀 🏢 (토글)
- 거리뷰: 세로 버튼 3개, 도로 클릭 → 360° 로드뷰
- 지도/스카이뷰/거리뷰 + +/- 우하단
- 모바일 반응형: 하단 탭바, 하단 시트, 햄버거 메뉴

### 5. PRO 매물안내서
- 단일 컬럼 폼, 소재지선택/단지명검색 탭
- 방향 8방위 드롭다운, 입주가능일 캘린더
- 가격 가로 3열, 기본 미체크
- 사진 5장, 직접입력 모드
- 즐겨찾기 무료 2개/PRO 무제한
- 관리자 배너 자동 삽입 (인쇄 보호 포함)

### 6. 어드민 (3탭)
- 🏠 중개사 / 🏢 업체 / 📢 배너

---

## 🎯 다음 작업

### 즉시
- [ ] **build_pyeong.sql / build_metrics.sql 재실행** (재워밍으로 채워진 실거래를 평형·시세 필터에 반영 — Supabase SQL Editor)
- [ ] 애드센스 심사결과 대기 (pub-3915435017395988, 검토요청 완료) → 승인 시 수동 광고단위 배치
- [ ] Supabase에 businesses/banners 테이블 생성

### 우선순위 높음
- [ ] **PRO 공문서 한번에 뽑기** — 진행중 (아래 현황 참조)
- [x] 용적률/건폐율 — `getBrTitleInfo` vlRat/bcRat 연동 완료, Supabase 캐싱
- [ ] PRO 중개사 Supabase Auth 실제 연동
- [ ] coords_all_apt.json 경량화 (build_lite.js → 8~10MB→2MB)

### 향후
- [x] 주상복합 지도 추가 — coords_all_apt.json에 aptType='주상복합' 1,552개 포함(지도·검색·SEO·실거래 정상, 아파트 API로 커버)
- [ ] 오피스텔 추가 — **진행중**: `files/generate_officetel.js`(실거래 기반) 작동 확인(아산/천안 테스트 149개). data.go.kr 오피스텔 매매/전월세 API 활용신청 완료. 남은 건 ①전국 확대(일일한도로 ~6일 분할) ②지도 연결(coords_ofi 로딩+apt중복 스킵+nameUnknown 구분). 상세는 변경이력 #7
- [ ] 온비드 공매 API 연동
- [ ] **입주 안내서 인쇄** — 아파트 클릭 → 입주 시 필요한 연락처 한 장 인쇄(매물안내서와 같은 결). 항목: 관리사무소 전화 / 전기 / 가스 / 수도 (+선택 인터넷). 출처: 관리사무소=K-apt `kaptTel`(실제 들어오는지 한 단지로 확인부터), 전기=한전 123(전국 고정), 가스=지역 도시가스사·수도=지자체(시군구별 표 필요, 아산·천안부터). 사용자 요청 2026-06-19 → 나중으로 미룸
- [ ] 청약홈 분양정보 API 연동
- [ ] 연립다세대 수집 (generate_villa.js)
- [ ] PWA 설정
- [ ] 실거래가 데이터
- [ ] **핀 위치 수정(보정) 도구** — 단지 핀을 수동으로 가운데로 보정 (아래 "📋 핀 위치 수정 도구" 스펙 참조). 사용자 요청(2026-06-14): 핀이 지번 기준이라 단지 끝/이상한 곳에 찍힘. ⚠️ 자동 재배치는 엉뚱한 곳 갈까봐 불안해함 → **수동 중심 + 전부 되돌리기 가능**으로 설계.

---

## 📋 PRO 공문서 한번에 뽑기 — 구현 스펙

### 개요
PRO 매물안내서 화면에 "공문서 조회" 섹션 추가.
주소 입력 → 6종 공문서 버튼 → 클릭 시 각 기관 페이지 열거나 API 데이터 표시.

### 서류별 방법

| 서류 | 방법 | API/URL | 필요 파라미터 |
|------|------|---------|--------------|
| 등기부등본 | 링크 | `https://www.iros.go.kr/pos1/jsp/main.jsp` | 주소로 검색 (자동완성 불가, 새탭으로 열기) |
| 건축물대장 | API | 건축HUB `getBrRecapTitleInfo` | sigunguCd, bjdongCd, bun, ji |
| 토지대장 | API | 공공데이터 `getLandCharacterStiInfo` | pnu (필지고유번호) |
| 토지이용계획확인서 | API or 링크 | 토지이음 `getLandUseInfo` or `https://www.eum.go.kr` | pnu |
| 지적도 | 링크 | `https://map.ngii.go.kr/ms/map/NlipMap.do?beforejump=1` | 주소 파라미터 |
| 임야도 | 링크 | `https://map.ngii.go.kr/ms/map/NlipMap.do?beforejump=1` | 주소 파라미터 |

### PNU 코드 계산법
PNU = 법정동코드(10자리) + 토지구분(1: 토지, 2: 임야) + 본번(4자리, 앞에0패딩) + 부번(4자리, 앞에0패딩)

카카오 주소검색 API 응답 → `address.b_code` (10자리 법정동코드)
지번 본번/부번 → `address.main_address_no`, `address.sub_address_no`

```javascript
// PNU 조합 예시
function buildPNU(bCode, mainNo, subNo, isForest=false) {
  const landDiv = isForest ? '2' : '1';
  const main = String(mainNo||0).padStart(4,'0');
  const sub  = String(subNo||0).padStart(4,'0');
  return bCode + landDiv + main + sub; // 19자리
}
```

### 건축HUB API 파라미터 추출
```javascript
// 법정동코드(10자리) → sigunguCd(5자리) + bjdongCd(5자리)
const sigunguCd = bCode.slice(0,5);
const bjdongCd  = bCode.slice(5,10);
// bun/ji = 본번/부번
```

### 토지이음 API
```
GET https://www.eum.go.kr/web/lu/luLandUseRqst.do
파라미터: pnu={PNU}, numOfRows=10, pageNo=1
※ 공공데이터포털 key 필요: getLandUseAttr (서비스키 별도 신청)
```

### 구현 위치
`admin/index.html` PRO 탭 또는 매물안내서 페이지 우측 패널
→ 주소 입력(카카오 주소검색 연동) → PNU 자동 계산 → 버튼 6개

### 등기부등본 링크 방식
등기부등본은 무료 API 없음. 인터넷등기소 새탭으로 열어주고 주소를 클립보드에 복사해주는 방식 사용:
```javascript
navigator.clipboard.writeText(addr);
window.open('https://www.iros.go.kr/pos1/jsp/main.jsp','_blank');
toast('주소가 복사되었습니다. 인터넷등기소에서 붙여넣기 하세요.');
```

---

## 📋 핀 위치 수정 도구 — 구현 스펙 (나중에)

**목적**: 단지 핀이 주소(지번) 기준 지오코딩이라 단지 끝/모서리에 찍히는 경우 많음 → 가운데로 수동 보정.

**⚠️ 사용자 우려(중요)**: 자동 일괄 재배치는 엉뚱한 곳으로 갈까봐 불안해함. → **자동 일괄 적용 금지. 사용자가 직접 놓은 것만 반영. 항상 되돌리기 가능**하게 설계할 것.

**설계**:
- **저장(핵심)**: 원본 coords(split_output/coords_all)는 그대로 두고, Supabase 신규 테이블 `apt_coords_override`(kapt_code PK, lat float8, lng float8, updated_at)에 **고친 좌표만** 저장. 프론트(index.html)는 로드 시 override 1회 조회 → MAP_DATA 빌드 시 해당 code만 override 좌표로 덮어씀. → 거대 파일 안 건드림, 되돌리기=행 삭제.
- **관리자 도구**(master 또는 admin 탭): 단지 목록을 순서대로(지역 필터, 예: 아산부터) 넘기며 → 카카오맵에 **draggable 마커** 표시 → 끌어서 위치 잡고 [저장](override upsert). 버튼: [다음/이전], [완료 표시], [원래대로](override 행 삭제).
- **(선택) '추천 위치' 버튼**: 누르면 카카오 키워드(장소)검색으로 단지명 좌표를 받아 마커를 그 지점으로 **점프만**(자동 저장 X). 수락하면 [저장], 아니면 직접 드래그. 안 누르면 작동 안 함. 가드레일: 원본에서 너무 멀면(>500m) 경고.
- **안전장치**: 자동 반영 절대 없음 · 원본 보존 · override 삭제로 즉시 복구.
- **현실 체크**: 전국 21,727개 수동은 개당 10초만 잡아도 ~60시간 → 지역 우선/중요 단지 위주 권장. 자동개선은 '추천 버튼' 형태로만(사용자 검토 후 적용).
- **RLS 주의**: 지도 적용 위해 override는 anon read 필요. write(insert/update)는 admin만 이상적이나 현재 Supabase Auth 미연동 → 임시로 anon write 허용+어드민 비번 뒤 배치, 또는 Auth 연동(다음작업) 후 제한.

---

## 📁 파일 현황

| 파일 | 상태 |
|------|------|
| split_output/ | 245개 파일, 21,825개 단지 |
| coords_all_apt.json | 21,825개 (8~10MB, 경량화 필요) |
| index.html | 최신 (사이드바 필드 수정, 모바일 반응형) |
| admin/index.html | 최신 (3탭) |
| fill_from_excel.js | 완료 |
| add_missing_apts.js | 완료 (513개) |
| retry_missing_apts.js | 완료 (1개 추가) |
| fix_manual_apts.js | 완료 (3개 추가) |
| fill_extra.js | 완료 |
| fill_extra_retry.js | 완료 |
| build_lite.js | 대기 (경량화용) |
| api/apt.js (detail) | 용적률/건폐율 Supabase 캐싱 + getBrTitleInfo 연동 완료 |
| api/diag.js | ✅ 신규 (district_cache 캐싱, metalist, fixmeta) |
| api/populate.js | 일괄 수집용 — 실행 후 삭제 예정 |
| supabase_district_cache.sql | ✅ 생성됨 — Supabase에서 실행 필요 |
| supabase_specialist.sql | ✅ 생성됨 — broker_apts 테이블 |
| master/index.html | ✅ 전면 재작성 완료 (5탭) |
| admin/index.html | ✅ 담당단지 기능 추가 |
| scripts/populate.js | 일괄 수집 로컬 실행 스크립트 |
| 20260605_단지_기본정보.xlsx | C:\Users\essoz\Desktop\files\ |

---

## 🔧 기술 메모

**📍 일반 핀(아파트/오피스텔/연립) 크기 프리셋 — 되돌리기용** (index.html `addMarker`의 nameCell·uCell·box.style·tip.style·wrap.filter 값만 교체):

| 프리셋 | 이름칸 padding / 글씨 | 세대수칸 padding / 글씨 | box radius | 꼬리(L·R / top) | shadow blur |
|--------|------|------|------|------|------|
| 이전(원래) | `2px 8px` / 11px | `1px 8px 2px` / 9.5px | 5px | 5px / 6px | `0 2px 3px` |
| **A (현재 적용 ← 2026-06-19 B에서 되돌림)** | `3px 9px` / 11.5px | `2px 9px 3px` / 11px | 5px | 5px / 6px | `0 2px 3px` |
| B (제일 큼) | `4px 10px` / 12.5px | `3px 10px 4px` / 12px | 6px | 6px / 7px | `0 2px 4px` |

공통(프리셋 무관): 이름칸 배경=TYPE_COLOR(아파트#3b82f6·오피스텔#a855f7·연립#10b981)+흰글씨, 세대수칸=흰바탕+#1a1a1a 글씨, 꼬리색=세대수 있으면 #fff·없으면 타입색, nameUnknown 오피스텔=점선 테두리, 분양권 핀 별도. "A로/이전으로" 요청 시 위 표 값으로 교체.

- git은 **GitHub Desktop** 사용 (사용자 선호). Git Bash도 가능
- K-apt API: HTTPS 필수, 일일 1만건, 자정 초기화
- 카카오 지오코딩: `화성만세구/효행구/동탄구` → 카카오 미인식 (화성시로 변환 필요)
- 포항북구/남구 → `포항시 북구/남구`로 변환 필요
- 엑셀 주소가 콤마로 여러 개인 경우 첫 번째만 사용
- MAP_DATA에 필드 추가 시 openAptSidebar/openAptDetail 둘 다 확인
- placeholder 예시에 사용자가 말한 단어 그대로 쓰지 말 것
- 스크립트: `cd C:\Users\essoz\GitHub\guhaedo && node 파일명.js`
- V4 API(`getAphusBassInfoV4`)에 용적률/건폐율 필드 없음 → `getBrTitleInfo` 사용. 총괄표제부(`getBrRecapTitleInfo`)는 아파트 단지에 totalCount=0으로 못 씀
- Supabase `apartments` 테이블: far, bcr, heat_name, use_date, sale_name 컬럼 추가됨. far+bcr 둘 다 있을 때만 캐시 반환
- populate 완료 후 `api/populate.js` 삭제하고 Vercel에서 POPULATE_SECRET 환경변수도 삭제할 것
- Git Bash push: `cd /c/Users/essoz/GitHub/guhaedo && git add . && git commit -m "메시지" && git push`
- **분양 핀 수정 시 presaleDup() / rebuildPresaleActive() 반드시 유지** — Opus 설계, 임의 교체 금지
- **Claude 샌드박스에서 git 명령 절대 실행 금지** — `.git/index.lock` 생성으로 GitHub Desktop 커밋 막힘
- 삭제 시: CMD `del /f "C:\Users\essoz\GitHub\guhaedo\.git\index.lock"`

---

## 🗒️ 변경 이력

- [2026-06-03] (작성: Claude) 인수인계서 LIVING DOC 신규 작성
- [2026-06-04] (작성: Claude) PRO 매물안내서 전면 리뉴얼. 어드민 3탭. fill_extra.js 작성
- [2026-06-05] (작성: Claude) 거리뷰, 업체핀, 모바일 반응형, 추가데이터 완료
- [2026-06-07] (작성: Claude) 엑셀(20260605) 데이터로 20,480건 보완(현관구조/시공사/주차/세대수). 누락 아파트 517개 카카오 지오코딩으로 추가. 사이드바 totalPark/jibunAddr/builder/entrance MAP_DATA 연동. 매물안내서 총주차 항목 추가. coords_sigungu.json 누락 18개 시군구 추가(불당동 포함). 학교 핀 크기 확대+이름 레이블 추가. PRO 공문서 뽑기 스펙 정리. api/land.js 생성. 건축물대장+토지대장 API인쇄 기능 완성(docPrint 팝업 창).
- [2026-06-08] (작성: Claude) 지도 안뜨는 문제(index.html 파일 잘림) 수정. 용적률/건폐율 연동: V4 API에 kaptFar/kaptBcr 없음 확인 → getBrTitleInfo(표제부) vlRat/bcRat 사용으로 변경. bjdCode/kaptAddr를 V4 응답에서 직접 추출(프론트 파라미터 불필요). Supabase apartments 테이블에 far/bcr/heat_name/use_date/sale_name 컬럼 추가 및 캐싱 로직 구현. api/populate.js + scripts/populate.js 생성(21,276개 일괄 수집). Vercel에 POPULATE_SECRET 환경변수 추가. 실행: `cd C:\Users\essoz\GitHub\guhaedo && node scripts/populate.js`
- [2026-06-11~13] (작성: Claude Sonnet) 실거래 면적 필터 소수점 정밀 구분(String key/Math.abs 비교). 전유부분 평수 표기 제거(공급면적 없음). A/B suffix 제거. .gitignore 생성. broker_apts 테이블+어드민 담당단지 UI+사이드바 전문중개사 카드. master/index.html 5탭 전면재작성. api/diag.js 신규(district_cache 캐싱). supabase_district_cache.sql 생성. 분양 핀 presaleDup 시스템 Opus 설계 유지.
- [2026-06-14] (작성: Claude Opus) 사전적재 백필 0% 멈춤 원인 규명: api/diag.js가 분양권(SilvTrade) 간헐 타임아웃을 그 달 전체 실패로 처리 → build.mjs가 매 시군구 첫 달(현재월)에서 중단 → progress.json 전 커서 202607 고정, trades 파일 0개. diag.js에서 분양권을 선택 데이터로 격리(매매/전월세 성공 시 캐싱+수집 진행, preError 플래그). trade.js 배치도 errCore/errPre 분리. 두 파일 node --check 통과. 사용자 1차 푸시 완료, diag 수정 라이브 확인(봉화 200601 → 매매 정상+preError, apiError 없음).
- [2026-06-14 #2] (작성: Claude Opus) 신축 단지 "거래내역없음" 원인 규명: 분양권 API 무응답 → /api/trade 함수가 Vercel 제한시간 초과(504) → 빈 응답 → 프론트 "거래내역없음". 라이브 재현(삼일파라뷰 A10022814: 캐시월만 요청하면 정상, 미보유월 섞이면 빈 응답). 수정: trade.js 분양권 3초 fail-fast+매매전월세 5초+동시처리 12, diag.js 분양권 재시도 제거(4초 fail-fast)+매매전월세 7초. v9의 diag 15초 재시도는 백필 504 위험이라 철회.
- [2026-06-14 #3] (작성: Claude Opus) 경주두산위브트레지움(A10024512) "거래내역없음" 추가 진단: 매칭 정상(addrMatch=true)·국토부 원본 거래 존재인데 apt_transactions에 빈 값 박제(오염 캐시). trade.js 캐시 버전컷 2곳 2026-06-14T07:00:00Z로 상향해 전체 무효화. 2차 푸시 후 라이브 검증 완료(경주 단지 매매25/전세2/월세3건 정상 표시).
- [2026-06-14 #4] (작성: Claude Opus) ① guhaedo-data Actions에서 백필 수동 실행(#6) 시작 — 전국 250 시군구, 2006(START_YM=200601)부터 백필. ② 단지 클릭 시 차트 기본범위를 **항상 2006년~현재(전체 이력)** 로 변경(index.html ~2044줄 `fromY=2006`, 정보 정확성 점검 목적). ⚠️ 백필 완료 전 미적재 단지는 라이브로 ~240개월 점진 로딩(느림/차트 갱신)되고, CDN 적재 후엔 즉시. **index.html 푸시 필요.**
- [2026-06-14 #5] (작성: Claude Opus) ① 백필 런 #6 결과: "성공"이나 진척 0(전 커서 202607 유지) — 옛 diag(15초 재시도) 버전으로 돌아 Vercel 504 추정. 현재 diag는 fail-fast 확인됨(종로 202606 라이브 정상, apiError 없음). 런 #7 진행 중 → 끝나면 커서 이동 확인 필요. ② **매일 09:00(KST) 백필 진행 자동점검** 스케줄 등록(task: guhaedo-backfill-check, progress.json 집계 보고). ③ **단지 제보 기능 추가**: index.html에 🚩단지제보 플로팅버튼+모달(단지명/위치/메모 → Supabase REST POST), 신규 테이블 `apt_reports`(supabase_reports.sql, ⚠️ Supabase에서 실행 필요). 검색 0건 시 검색어를 제보 기본값으로. 참고: 1차/2차는 데이터에 별도 존재(좌표 근접→핀 겹침, 경미). **index.html + supabase_reports.sql 푸시 + SQL 실행 필요.**
- [2026-06-14 #6] (작성: Claude Opus) "핀 없는 아파트" 원인 규명: 지도 데이터 출처가 K-apt(의무관리대상 공동주택만)라 소규모(150세대 미만)·구축 자체관리·연립/빌라·오피스텔은 통째 누락. 예) 경주 금장로얄아파트 — 우리 데이터·국토부 실거래(4개월)·카카오 주소검색 모두 미검출 = 소규모/구축 추정. 보강책: 실거래에서 단지 추출 or 제보→수동 지오코딩. 그리고 **핀 위치 수정 도구**를 '향후 작업'+스펙으로 등록(수동 드래그+override 테이블+되돌리기, 자동 일괄 금지 — 사용자 우려 반영). 이 세션은 스펙만, 미구현.
- [2026-06-14 #7] (작성: Claude Opus) 오피스텔 추가 작업 시작(실거래 기반). `files/generate_officetel.js` 신규 — 국토부 오피스텔 매매/전월세 실거래(2006~현재)에서 건물 추출→카카오 지오코딩→`split_output/coords_ofi_{코드}.json`(type:'officetel'). 사용자가 data.go.kr 오피스텔 매매/전월세 API 활용신청 완료(키 동일). 테스트 성공(아산57+천안서북92=149개, 좌표 정확, 옛날 것 포함). 버그수정: 응답 Buffer.concat 디코딩(한글깨짐+중복 동시 해결), 이름없는 소규모는 'OO동 오피스텔 (지번)'+`nameUnknown:true`로 구분(나중 이름채움용). **남은 일**: ① 전국 확대(SIGUNGU_LIST 250개+resume/일일한도 분할 — 246개월×2API, 시군구당 ~500콜→전국 ~6일) ② 지도 연결(index.html이 coords_ofi 미로딩 → 로딩 추가 + apt 핀과 좌표 근접 시 스킵 + nameUnknown 핀 구분표시) ③ 배포.
- [2026-06-14 #8] (작성: Claude Opus) 오피스텔 지도 연결(테스트 단계, A안). index.html: `loadAptData`가 같은 시군구 `coords_ofi_{코드}.json`도 로드(신규 `loadOfiData`) → 아파트 핀과 **40m 이내 겹치면 스킵**(주상복합 중복방지), `nameUnknown` 오피스텔은 **점선 테두리**로 구분. generate_officetel.js: 이름없는 건 'OO동 오피스텔 (지번)'+`nameUnknown:true`, **OUTPUT_DIR을 `C:/Users/essoz/GitHub/guhaedo/split_output`로 변경**(재실행 시 레포에 바로 저장→푸시만). 수정구간 node --check 통과(전체 index.html은 CRLF/추출 이슈로 부분검증). **다음: 사용자 재실행(아산44200·천안서북44133)→푸시(index.html + coords_ofi 2개)→지도 줌인해 보라 핀 확인. 좋으면 전국(resumable+일일한도 6일 분할).**
- [2026-06-14 #9] (작성: Claude Opus) 일반 핀(아파트/오피스텔/연립) 디자인 개선 — 사용자가 시각 예시 5종 중 '옵션1 변형A' 선택. addMarker 핀 구조 변경: 이름칸=타입색(아파트#3b82f6/오피스텔#a855f7/연립#10b981)+흰글씨, **세대수칸=흰 바탕+검정 글씨(별도 칸)**, 아래 **뾰족한 꼬리(삼각 포인터)**, drop-shadow, yAnchor 1.1→**1.0**(꼬리 끝이 좌표 가리킴). nameUnknown 오피스텔 점선 테두리 유지. **분양권 무지개 핀은 그대로**(renderPresalePins 미변경). 세대수 글씨 작다는 피드백 → 크기 B 적용(이름 12.5px·세대수 12px·꼬리 6/7px). 수정구간 node --check 통과. **index.html 푸시 필요.**
- [2026-07-13] (작성: Claude Sonnet) 핀 등장 bounce 애니메이션(pinPop) 제거 — 사용자 요청(눈아픔). CSS/@keyframes/className/animationDelay 전부 삭제. 실거래 24년 12월 이후 없는 문제 → 코드 정상, 실거래 없는 것으로 결론. Claude Fable 5 미국 수출통제로 전 세계 차단 (Opus 4.8 사용).
- [2026-06-14 #10] (작성: Claude Opus) **오피스텔 전국 수집 본 스크립트**(files/generate_officetel.js 전면 교체): ① 실거래(2006~) 건물추출 ② 카카오 지오코딩(좌표+법정동코드 b_code) ③ **건축물대장 표제부(getBrTitleInfo)로 세대수(호수)·용적률·건폐율·층수·준공 보강**(아파트와 같은 양상). 시군구목록은 split_output/coords_apt_*.json에서 자동로드. **일일예산(OFI_BUDGET 기본 9000월) 도달 시 중단+progress(ofi_progress.json) 저장 → 재실행 시 이어서**(전국 ~6~7일). 결과 guhaedo/split_output/coords_ofi_{코드}.json(units=호수를 '세대'로 기록). index.html `loadOfiData`: 오피스텔 핀 units→'○○세대' 표시 + far/bcr/topFloor 캐리. 스크립트 node --check 통과(샌드박스 해외IP라 실데이터 미검증 → **첫 시군구 결과로 세대수 확인 예정**; 건축HUB 호수 필드 hoCnt/hhldCnt/fmlyCnt 실응답 확인 필요). **index.html 푸시 + 수집한 coords_ofi 파일들 푸시.**
- [2026-07-15] (작성: Claude Opus) **a99.co.kr 도메인 오픈 + 수익화(검색 SEO) 방향 확정.** ① 도메인: a99.co.kr(아·9·9) 구매→Vercel guhaedo 프로젝트에 도메인 추가(apex, www리다이렉트 OFF)→가비아 DNS A레코드 `@ → 216.198.79.1`→**Valid Configuration, SSL 자동**. 카카오 JS SDK 도메인(비즈앱 ID 1462999, JS키 a7a6…)에 `https://a99.co.kr` 추가→**지도 정상 렌더 확인**. (⚠️카카오 로그인도 a99에서 쓰려면 같은 화면 '카카오 로그인 리다이렉트 URI'에 a99 추가 필요 — 미완). ② **구조 확정: a99.co.kr=실서비스(앱+검색페이지 공존), guhaedo.kr=테스트. 코드·데이터·레포는 하나**(둘로 안 쪼갬 — 섞임/드리프트 방지). ③ **수익모델=올도구식**: 공공데이터 단지 정보페이지(검색 유입용 '도어웨이')→광고(애드핏→애드센스)+`?apt=코드` 버튼으로 지도 앱에 유입해 사용량↑. **검색페이지는 앱 UI엔 노출 안 함**(사용자 안건). 강등방지 3원칙: 정보 안 가림 · 전면광고는 페이지 이동 시 1회만 · 콘텐츠>광고 (구글 intrusive interstitial 강화 + 네이버 C-Rank/D.I.A·저품질 근거). ④ **관리사무소 전화번호 수집 착수**: split_output·지도에 전화번호 없음 확인 → K-apt `getAphusBassInfoV4` 응답의 `kaptTel`로 수집 가능(api/apt.js가 이미 쓰는 API, 별도 신청 불필요). 신규 `scripts/collect_kapt_tel.js`(전국·resumable·BUDGET 기본 9000/일 → 결과 `split_output/kapt_tel.json`={code:{tel,fax}}, 전국 ~3일). ⚠️**사용자 PC에서 `node scripts/collect_kapt_tel.js` 실행 필요**(샌드박스 해외IP라 K-apt 차단). ⑤ 참고 산출물(a99 폴더 `a99_demo/`): 수익화_계획서 v2.1, SEO 생성기 데모 `a99_generate.js`+샘플페이지, 지도 `?apt=` 핸들러 스니펫, businesses/banners SQL. **다음 작업: ①전화번호 수집 실행 ②생성기를 실데이터+전화번호로 연결해 단지 검색페이지 생성 ③배포(GitHub Desktop push) ④구글 서치콘솔·네이버 서치어드바이저 등록+sitemap ⑤애드핏 코드 삽입.**
- [2026-07-17] (작성: Claude Opus) **충남 검색페이지 실제 출시 + 구글·네이버 검색등록 + 애드핏 광고 설치(MVP 런칭).** ① 전화번호 전국 수집 완료 → `split_output/kapt_tel.json`(코드→{tel,fax}, 041-XXX-XXXX 형식). ② `scripts/build_seo_pages.js`로 **충남(`set SGG=44`) 1,001개 단지 검색페이지 생성** — 관리사무소 전화번호 + 기본정보 + "🗺️ 지도에서 보기" 퍼널 + 광고자리. 출력 `apt/{code}.html` + `apt/sitemap.xml`. (SGG=시군구코드 앞자리, 44=충남 / 생략=전국) ③ **`vercel.json` `{cleanUrls:true}`** 추가 → `/apt/{code}` 깔끔 URL, canonical 일치. ④ GitHub Desktop push 배포 → **a99.co.kr/apt/{code} 라이브 확인**(예: A10020112 애플타운 나임아파트 041-333-4011 정상). ⑤ **구글 서치콘솔**: URL접두어 `https://a99.co.kr`, index.html에 `google-site-verification` 메타태그 → 소유확인 완료, sitemap `apt/sitemap.xml` 제출. ⑥ **네이버 서치어드바이저**: `naver-site-verification` 메타태그로 소유확인 완료(⚠️ 네이버는 크롬MCP 안전제한으로 Claude가 접속 불가 → 사용자가 직접, 메타태그·sitemap 값은 Claude 제공). ⑦ **애드핏**: 매체 등록(아구구/Web/a99.co.kr/비즈니스) + 광고단위 300x250 **DAN-VVNFKrpvthFpw6PS** 생성 → 코드를 build_seo_pages.js '자동광고 자리'에 삽입. 배포 후 광고호출 발생 시 **매체 심사 1~2일**. ⚠️ **index.html에 구글·네이버 메타태그 2줄 추가됨(head, 삭제 금지).** **다음: ①애드핏 코드 반영 위해 재생성·재푸시(`set SGG=44&& node scripts/build_seo_pages.js` → push) ②전국 확대(SGG 빼고 전체 — 파일 많으면 serverless 검토) ③관리비 수집(3개월/1년 평균, data.go.kr 활용신청 필요) ④네이버 sitemap 제출 확인 ⑤지도 `?apt=` 딥링크(퍼널 자동열기, index.html openAptDetail(객체) 연동).**
- [2026-08-31] (작성: Claude Opus) **실거래/관리비/평형·시세/브리핑 시스템 정리 + SEO 실거래삽입 + 애드센스 신청 (상세는 상단 v14 섹션).** ① 실거래 매칭 개선 2건 커밋: 시행사 접두사((주)/㈜/(유)) 제거 매칭(호려울마을10단지 등 6개), 신도시 '○○마을N단지' 정준식별자 매칭(세종·은평뉴타운 29개) — trade.js+warm.js 동기수정, 영향 단지 캐시 과거화(자가치유 재발동). 재워밍 100% 완료·화성類 정상 확인. 전수 census 통과. ② SEO: api/aptpage.js가 apt_metrics 읽어 **실거래·시세·FAQ·meta desc를 서버HTML에 삽입**(빈약페이지→알맹이, 색인강화) 배포. IndexNow 주간크론 추가(vercel.json). 단지제보 FAB 제거→헤더/모바일메뉴. ③ 매물안내서 분양·미입주 직접입력 개선(검색없음 CTA·직접입력 우선·code=''). 브리핑 짧은링크+카톡OG 검증(201 OK). ④ **애드센스 신청**: pub-3915435017395988 인증메타+스크립트(index/aptpage head)+ads.txt, 사용자 검토요청 완료(심사대기). GDPR 메시지 로고필수로 게시보류(승인·수익 무관). ⑤ 애드핏 수익하락 진단=무효클릭 회수(옛 CTR 2~3.5% 비정상→걸러짐), 실제 baseline 낮음, 광고 정상. SDK도메인 공지(516)=a99 이미 신규도메인 무관. ⑥ 색인현황: 구글 691/21,620(3%, 재크롤 대기), 네이버 크롤 정상(500~640/일). ⚠️ 모든 미푸시 없음(배포완료). **다음: build_pyeong/build_metrics SQL 재실행(재워밍 반영), 애드센스 심사결과 대기, 우선단지 GSC 색인요청.**
- [2026-07-17 #2] (작성: Claude Opus) **전국 서버리스 단지페이지 + 관리자 보안 통합 + 지도 딥링크 + 초반 무료 전략 확정.** ① **전국 서버리스**: `api/aptpage.js` 신규 — `split_output/coords_all_apt.json`(전국 21,727)+`kapt_tel.json` 읽어 `/apt/{코드}` 즉석 렌더(BY_CODE Map, 파일 2만개 안 만듦). `vercel.json`에 rewrite `/apt/:code → /api/aptpage?code=:code`(정적 충남 우선, 나머지 서버리스). `apt/sitemap.xml` 전국 21,727 URL 재생성. 서울 경희궁의아침4단지 라이브 정상(02-732-4975). ② **관리자 보안**: `admin`·`master`·`aptfix` → **`admin-vkz`·`master-vkz`·`aptfix-vkz`**로 폴더명 변경(URL 유추 방지), 비밀번호 전부 **`agugu9427`**(기존 8888/9999), 각 페이지 `<meta robots noindex>`, `robots.txt` 신규(관리자 경로 미노출·sitemap만 명시). **통합관리자 `admin-vkz/hub.html` 신규**(아실식 단일 관리자 방향): 로그인 게이트(agugu9427)+5탭(중개사·업체·배너/진단/핀수정/회원·중개사 관리/설정)+중개사 가입폼(개인정보 수집·이용 동의[필수]+마케팅[선택], submitSignup=Supabase brokers 연결 TODO). 진단·핀수정 탭은 현재 placeholder+기존 페이지 링크. 관리자 이모티콘 전부 제거. ③ **지도 딥링크**: `index.html`에 `handleAptDeeplink()` — `?apt=코드` 진입 시 allAptData(전국) 조회→지도 이동(setLevel4+setCenter)→`loadAptData(시군구코드)` 마커→`openAptDetail` 자동 오픈, window.onload에서 호출. node --check 통과, SEO 정적·서버리스 둘 다 `/?apt=코드` 링크 일치 확인. (대안: URL에 좌표 실으면 13.9MB 대기 없이 즉시 — 미적용/선택.) ④ **초반 무료 전략 확정**: 중개사 상품은 **무료로 풀어 회원·트래픽 확보**, 광고(애드핏)로 수익 유지, 서비스 성숙 후 유료 전환. 아실 영업모델 참고(단지별 중개사 **2곳 독점·1년 회원제** = Engine A 중개사 프리미엄 모델 그대로 적용 가능). **매물안내서 프리미엄(계획/미실행)**: 가입+광고=무료, 광고제거 유료 **1년 77,000→38,500 / 6개월 44,000→28,600**. ⚠️ **위 관리자 변경·hub·딥링크 전부 아직 push 안 함 → GitHub Desktop push 필요(Vercel 자동배포).** **다음: ①전체 push ②hub 기능 실제 이관(중개사·진단·핀수정) ③hub 가입폼→Supabase brokers 연결 ④지역광고 시스템(businesses/banners SQL 실행+배너폼) ⑤관리비 수집(3개월/1년 평균, data.go.kr 활용신청).**
- [2026-09-02] (작성: Claude Opus) **아실(asil.kr) 벤치마킹 실측 + 지도 고도화 로드맵 확정 (상세는 상단 v16).** 브라우저로 직접 열어 Network/DOM 분석: 아실=JSP서버렌더+jQuery+네이버지도, 지도API가 JSON 아닌 **실행가능 JS문자열**(addCustomOverlay 나열) 반환해 핀HTML을 서버가 생성. 줌3단계 bbox쿼리(시도/읍면동/단지)+aptcount(필터통과 개수만), 필터13종·핀표시모드12종 전부 서버처리 → coords 프론트 미전송. 오버레이 교통망·재개발·학군, 지도 경매핀, 단지상세 이중선차트+기간슬라이더+VS비교+집내놓기, 홈 부동산스터디 20여종(=검색유입 해자). a99 우위=관리사무소 전화·관리비·매물안내서·**단지별 색인URL 21,727개**(아실은 javascript:라 단지URL 없음). ⚠️ **대표평형 선정 버그 발견**: 배방삼정그린코아(2,156세대)가 대표 38㎡·6,500만원·전세가율109%로 잡힘(build_metrics.sql이 거래 최빈면적을 대표로 씀) → 세대수 최다 평형 기준으로 변경 필요, **핀 가격표시의 선행조건**. 작업순서 0~8 확정(0 대표평형수정 → 1 핀모드 → 2 핀라벨 → 2.5 지도컨트롤 → 3 거래회전율 → 4 필터확장 → 5 bbox전환 → 6 차트 → 7 랭킹페이지 → 8 매물/경매). **사용자 확인사항**: 핀 라벨은 **세로 2단 유지**(가로 배치 반려, 파랑 이름 위/흰색 값 아래), 이름 접미 제거 방식 승인. **지도 컨트롤 개편 요청**: 지도+스카이뷰 통합 택1, 거리뷰·지적편집도 각각 on/off 토글 — 지도 수정 시 동시 진행 요구. 코드 복제 없음(패턴만 기록, 아실 클래스명·마크업 사용 금지).
- [2026-09-02 #2] (작성: Claude Opus) **0-a·0-b·1·2·2.5 구현 완료.** ⚠️ **앞선 '대표평형 버그' 주장은 오진이었고 정정함**(상단 v16-C): 배방삼정그린코아는 areaBreak u60=2156으로 전 세대 소형이라 대표 38㎡가 정답. 실제 문제는 ①역전세 갭이 `wonMan()`의 `man<=0 → '-'`로 지워지던 것(수정: 음수 부호 표기 + '역전세' 라벨) ②혼합단지 소수평형 쏠림(아산 11곳 중 1~2곳, 예: 한라비발디 u85 76%인데 대표 55㎡). ②는 `scripts/build_area_break.js` 신규 → `build_area_break.sql`(20,693단지, 531KB) 생성 + `build_metrics.sql`에 **밴드 가드**(세대수 최다 밴드 안에서 최빈, 없으면 기존 최빈 폴백) 추가. 함께 `turn_rate`(거래회전율)·`far`/`bcr` 컬럼 추가. index.html: 핀 표시모드 13종 드롭다운(`PIN_MODES`, `refreshPinMetrics`가 bbox로 apt_metrics 캐시), 핀 라벨 세로2단 유지하며 `shortName()` 이름축약 + 아랫칸 모드별 값, 지도컨트롤 `setMapBase`/`toggleRoadview`/`toggleDistrict` 분리(베이스 전환해도 오버레이 유지). 신규 컬럼 미생성 시 select 자동 폴백 넣어 **푸시 순서 무관하게 안전**. 인라인 script 6블록 전부 node --check/JSON 검증 통과.
- [2026-09-03] (작성: Claude Opus) **지적편집도 버그 수정 + 랭킹 페이지 5종 구축.**
  ① **버그 수정(배포·검증 완료)**: `setMapBase()`가 베이스 전환 후 오버레이를 다시 `addOverlayMapTypeId` 하는데, 카카오는 **같은 오버레이를 두 번 add하면 내부에 두 번 쌓여** remove 1회로 안 지워짐 → 지적편집도가 안 꺼졌음. `setOverlay(id,on)` 헬퍼(항상 remove 먼저 → 필요시 add)로 교체, 거리뷰도 동일 적용. 크롬 실측으로 지적도 타일 0개 확인.
  ② **build_metrics.sql 변동률 3종 추가**: `price_avg_m`(최근18개월 대표평형 **평균**), `price_prev_m`(30~18개월 전 평균), `chg_pct`, `deal_cnt_prev`, `chg_base_ym`. ⚠️ `price_m`은 '최신 1건'이라 변동률에 쓰면 저층 1건에 튐 → **평균끼리 비교**하도록 별도 컬럼. 랭킹 노출 조건 `deal_cnt>=3 AND deal_cnt_prev>=3`.
  ③ **랭킹 페이지**: `/rank`(인덱스) + `py`(평당가)·`gap`(갭)·`jrate`(전세가율)·`up`/`down`(변동률) × 시도 17 = **90 URL**. ⚠️ **Vercel 서버리스 함수가 정확히 12개로 한도 꽉 차 `api/rank.js` 신규 생성 불가** → `api/aptpage.js`에 병합(`?rank=&scope=`), `vercel.json` rewrite 3줄 추가. `hubHtml()` 껍데기 재사용.
  ④ **갭 랭킹 설계 주의(중요)**: 조건 없이 `gap_m` 오름차순으로 뽑으면 **전세가율 98~100% 깡통전세 목록**이 됨(실측: 하가휴먼빌·한일타운3차 갭 100만원/전세가율 100%). → **전세가율 40~85%만** 포함 + **가격대 3구간**(3억↓/3~6억/6억↑)으로 분리. 잘려나간 85%↑는 `/rank/jrate` "전세가율 높은 아파트(전세 계약 전 확인)"로 별도 콘텐츠화. 공인중개사 명의 페이지라 보수적으로 잡음.
  ⑤ **내부링크**: index.html 헤더에 '랭킹' 추가(애드센스 메일의 '탐색이 쉽고 메뉴·링크가 분명한가' 대응) + 푸터, `hubHtml` 푸터, **단지페이지 2만여 개에서 `/rank/py/{시도}` 역링크**(색인 상호 견인). `sitemap-main.xml`에 랭킹 90 URL 반영.
  ⑥ 신규 컬럼 미생성 시 `RANK_SEL_FULL→BASE` 자동 폴백 + 변동률 랭킹은 '준비 중' 표시 → **푸시 순서 무관하게 안전**. 로컬 라우팅 스모크테스트 8케이스 전부 통과(200/404, region·apt 회귀 없음).
- [2026-09-03 #2] (작성: Claude Opus) **지역 사이드바에 랭킹 진입 + 🔴 시도코드 버그 수정.**
  ① 🔴 **`SIDO_LIST` 시도코드 오류 (라이브 재현 확인)**: 강원도가 `42`, 전라북도가 `45`로 박혀 있었으나 실제 coords `sigunguCode`는 **강원 `51` / 전북 `52`**(강원특별자치도·전북특별자치도 개편으로 법정동코드 변경). `selectSido()`가 `startsWith(code)`로 거르는데 0건 → **사이드바에서 강원도·전라북도를 누르면 "데이터가 없습니다"**가 떴음. 라이브 콘솔 실측: 42→0건 / 51→842건, 45→0건 / 52→890건. 코드 2개 수정. **17개 시도 전부 데이터 매칭됨을 검증**(`SIDO_LIST` 코드 ↔ coords 실제 코드 대조).
  ② **사이드바 랭킹 블록**(아실 '부동산 스터디' 자리 = 지역 그리드 바로 밑): `#rs-rank` + `syncRsRank()`. **선택한 시도에 따라 링크가 바뀜** — 충남 선택 시 `/rank/py/44`. 시도 미선택이면 전국. 칩 6개(평당가·갭 작은·전세가율·상승·하락·전체), `target="_blank"`로 열어 지도 상태 유지. `renderRsGrid()`·`selectSido()`에서 호출.
  ③ 아실 대비 개선점: 아실은 고정 아이콘이지만 a99는 **지역 컨텍스트를 따라감**.
- [2026-09-04] (작성: Claude Opus) **Supabase SQL 전량 실행 완료 (Claude가 사장님 크롬으로 직접 수행).**
  ① `apt_area_break` 생성 + **20,693건 입력**(531KB SQL 붙여넣기 대신 브라우저에서 `allAptData`의 areaBreak를 PostgREST bulk upsert 3,000행×7회 — 전부 성공).
  ② 신규 컬럼 8개 + 밴드가드 재계산 + turn_rate + far/bcr + 변동률 완료.
     결과: 지표단지 **17,186** / 회전율 17,152 / **용적률 1,574(적음 — apartments.far 수집률 낮음, 별도 보강 필요)** / 역전세 **618** / 변동률 14,454 / 랭킹조건통과 8,120 / 최고 +122.9% · 최저 −54.0%.
  ③ 🔴 **타임아웃 대응(중요)**: `apt_transactions` 전체 **543,119줄**. 기존 SQL은 2006~ 전 줄을 lateral 펼친 뒤 필터 → Supabase 게이트웨이 타임아웃(약 2분). **`t.ym` 컬럼으로 펼치기 전에 자르도록 수정**(최근18개월 = 386,519줄, 29%↓) → 통과. ⚠️ **결과값은 100% 동일**(어차피 18개월 대표가 계산). 실측: 용산구 한 곳 전기간 펼치기 13.2초 / 배열 concat 최적화 11.0초(17%↓, 미미) → 병목은 jsonb 펼치기 자체.
  ④ 🔴 **`apartments.far`/`bcr`가 text** → numeric 캐스팅 필요(정규식으로 숫자꼴만). 그냥 대입하면 42804 에러.
  ⑤ 🔴 **`api/aptpage.js` 폴백 버그 수정**: `RANK_SEL`을 모듈 전역에 두어 한 번 폴백되면 서버리스 인스턴스가 살아있는 동안 계속 '준비 중' 표시 → **요청 단위 폴백**으로 변경(`rankQuery`가 `{rows, full}` 반환).
  ⑥ **평형(build_pyeong)**: 전 기간 필요(기간 축소 불가 — 평형 종류를 다 잡아야 함). 시군구 범위 3분할 + `not exists`로 **누락 단지만** 계산 → 18,350 → **18,776**(+426), `apt_pyeong_blob` 갱신 완료. 남은 2,964단지는 **면적값 있는 거래가 0건**이라 계산 불가(영구). ⚠️ `lawd_cd`에 인덱스 없어 청크마다 전체 seq scan — 전량 재계산은 약 40분 소요 추정, 대시보드로는 비현실적.
  ⑦ ⚠️ Supabase SQL Editor의 "Run without RLS / Run and enable RLS" 경고에서 **반드시 'Run without RLS'** — enable 누르면 anon 읽기가 막혀 지도·랭킹이 통째로 멈춤.
- [2026-09-04 #2] (작성: Claude Opus) **랭킹 UI 개편 — 메뉴 페이지 제거·즉시전환·왕관·기준일.**
  ① **`/rank` 메뉴 전용 화면 폐지**: 아이콘만 있고 한 번 더 눌러야 했음 → 이제 `/rank`·`/rank/all/{시도}` 모두 **평당가 순위를 바로 렌더**. `rankIndexPage()`는 미사용(코드는 남겨둠). 라우팅에서 `index|all → rankPage('py', scope)`.
  ② **레이아웃**: 아이콘6 + 지역칩을 페이지 **상단**(`hubNav()`), 구분선, 그 아래 제목·기준일·목록. 전체를 `#rkall`로 감쌈.
  ③ **페이지 이동 없이 전환**(`RANK_JS`): `#rkall` 안의 `/rank*` 링크 클릭을 가로채 같은 주소를 `fetch` → 서버가 만든 `#rkall`을 그대로 갈아끼움. **렌더 로직 중복 없음**, JS 꺼져도 링크로 동작, 크롤러는 각 URL을 그대로 읽음. `history.pushState`로 주소 갱신, popstate는 reload.
     ⚠️ `hubNav`의 `/region` 링크는 `/rank` prefix가 아니라 정상 이동됨(의도).
  ④ **순위 배지**: 1·2·3위 금·은·동 그라데이션+왕관, 4~5위 초록, 6~10위 연한 초록, 11위~ 숫자만. 1~3위 행 배경 옅게. CSS counter → `rankBadge(i)` 명시 마크업으로 교체.
  ⑤ **기준일 배지** `asOf()`: "📅 YYYY년 M월 D일 집계 · 최근 18개월 실거래 기준(+ 직전 1년 구간과 비교)".
  ⑥ **준공년차 선택** `AGE_OPTS`(전체/2/3/5/10년) — **상승·하락에만** 적용, 기본 **3년**. 주소에 `?age=` 없으면 3년, `all`을 골라야 제한 없음. 이유: 신축은 입주 직후 급매가 기준가가 되어 상승률이 부풀려짐(실측: 모아엘가 그랑데 2024년 준공 +122.9%, 1.9억→4.3억 — 3년 필터로 제외됨).
  ⑦ ⚠️ **`.hub` CSS는 `RANK_HUB_CSS`에 있으므로 랭킹 페이지에도 함께 붙여야 함**(`RANK_CSS + RANK_HUB_CSS + RANK_JS`). 빠뜨리면 아이콘이 깨짐.
  ⑧ **최소 매매가 조건은 넣지 않기로 결정(사용자 판단)**. 배경: 저가 단지가 변동률 상위에 오름(산북동 부향하나로1차 군산, 1,920만→2,300만 = +52.6%인데 실제 차액 380만원). 실측 영향 — 5,000만원 상한 시 대상 5,709→5,656개(−53)로 문제 해결, 1억/2억으로 올려도 **상위 순위는 동일**하고 대상만 최대 1,301개 감소(지방 소형 손실). **나중에 필요하면 `RANKS.up/down.base`에 `price_m=gte.5000` 한 줄 추가하면 됨.**

---

## 📌 다음 인수자 지시문 (DO NOT REMOVE)

작업 마칠 때마다:
1. **상태 갱신**: 완료작업/다음작업/파일현황/기술메모 중 바뀐 부분 수정
2. **이력 기록**: 변경이력 맨 아래에 `[날짜] (작성: Claude) 요약` 추가 (기존 삭제 금지)
3. **메타 갱신**: 최종갱신 날짜 + 버전 +1
4. **지시문 유지**: 이 블록 삭제 금지
5. 갱신 후 사용자에게 변경 요약 보고 + 파일 제시
