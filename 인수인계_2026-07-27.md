# 아구구(a99) 작업 인수인계 — 2026-07-27 (최신)

※ 이전 버전(2026-07-24)의 내용을 모두 포함하고 그 뒤 작업을 추가함. 이 파일이 최신.
※ 이번(07-27) 신규: **관리비 수집·표시 시스템** + **kapt_info.json 통합** — 8절 참고.
※ **2026-08-04 대규모 업데이트 — 아래 "9. 2026-08-04 세션" 참고 (실거래 매칭 대수술, 평형필터, 프리페치 등).**

---

## 9. 2026-08-04 세션 (실거래 매칭 대수술 + 평형필터 + 프리페치)

### 9.1 관리비 캐시 RLS 버그 (해결)
- 증상: `type=mgmtcost` 캐시가 매번 `cached:false`. 원인: **apt_mgmtcost 테이블 RLS가 켜져 있어 서버(anon 키) 쓰기 42501 차단.**
- 해결: Supabase에서 `alter table apt_mgmtcost disable row level security;` 실행 → 정상. (`await mgmtWriteCache`도 이미 적용됨.)
- **교훈: Supabase 새 테이블은 RLS가 켜진 채 생성됨. SQL 안의 `disable` 문이 안 먹는 경우가 있어 standalone으로 다시 실행해야 함.** SUPABASE_KEY는 anon급(RLS 적용됨, service_role 아님).

### 9.2 실거래 안 뜨는 단지 (~1,353개, 6.2%) — 매칭 버그들
전수조사 결과 21,718개 중 1,353개가 실거래 0건. 원인이 **여러 개**(하나 아님):
1. **괄호 브랜드 불일치**: 국토부 "○○3단지(브랜드)" vs 우리 "○○3단지". → `api/trade.js`·`api/warm.js` 매처에 규칙 추가: 괄호 떼고(`replace(/\([^)]*\)/g,'')`) 정규화한 이름이 정확일치 + 같은 법정동이면 지번 달라도 인정. (검증: 새샘마을3단지 127건.)
2. **행정구역 개편 코드오류(화성 등)**: K-apt는 옛 시군구코드(41591), 국토부 실거래는 새 코드(41597)를 씀 → 엉뚱한 지역 조회로 0건. → `api/trade.js`에 **형제 시군구코드 재시도**: 1차 매칭 0건이고 국토부에 데이터는 있으면(=유효코드지만 우리 단지 없음), 같은 시(앞4자리)의 다른 구코드 0~9를 프로브해서 매칭되는 코드로 재조회. (검증: 동탄호수자이파밀리에 41591→41597 17건, 오매칭 0.)
3. **0건 자가치유**: `api/trade.js` 배치 핸들러 — 캐시가 전부 0건이고 `fetched_at < 2026-08-04T12:00Z`면 재조회 강제(`batchMissing=전체`). 재조회하면 fetched_at 갱신돼 **1회만** 재시도(진짜 빈 단지도 무한반복 안 함).
4. **남은 것(미해결, 일부러 보수적)**: "부산전포동 사랑으로 부영" 같은 **지역명 접두** 패턴, "동탄호수공원아이파크"처럼 국토부에 없는 개별 케이스. 억지 매칭은 오매칭 위험 → 방치. **근본 해법은 9.5의 단지코드 매핑.**

### 9.3 재워밍(warm.js) — 개조 완료·재가동 중
- **warm.js 개조 완료(8-04):** 개편된 시(한 4자리 프리픽스에 구코드 2개+ = 수원·성남·용인·화성·청주·창원 등 17곳) 감지 → **형제 구코드 0~9 전부 조회대상에 추가**(`lawdSet`), 매칭은 **시(4자리) 단위 그룹**(`APT_BY_PREFIX`)으로, **데이터 있는 단지만 저장**(형제코드 태스크 중복 시 빈결과가 데이터 덮어쓰기 방지). → 화성 41597 등 국토부 신규코드까지 훑어서 proactive로 채움.
- 전체 태스크 6,048 → **9,000**(형제코드 추가분). 국토부 하루 3,000 태스크 한도 → **약 3일** 소요.
- **재가동함:** warm_state cursor=0 리셋 완료. GitHub Actions(20분마다)가 자동 진행. 진행률은 `a99-warm-progress-alert`(매일 9:05)로 통지.
- 참고: 이제 warm은 빈 결과를 저장 안 하므로, 진짜 거래없는 단지는 캐시 행이 없고 trade.js가 온디맨드로 처리(자가치유).

### 9.4 평형 9구간 필터 (완료, 라이브 작동)
- 요청: 10평미만/10평대/…/80평대~ 9구간, **공급면적 기준**, 다중선택.
- 데이터: `scripts/build_pyeong.sql` — `apt_transactions`에서 단지별 전용면적 → 공급평형 환산(전용률 0.75, `전용㎡/0.75/3.3058`) → 9버킷 → `apt_pyeong`(kapt_code→int[]) → 단일 blob `apt_pyeong_blob`(id=1, jsonb 18,350개). **주의: jsonb_object_agg에 괄호배열 union 문법 이슈 있었음 — 최종 SQL은 `select value as elem from jsonb_array_elements(...)` 형태로 수정된 버전 사용.**
- **RLS: apt_pyeong / apt_pyeong_blob 둘 다 `disable row level security` 필요**(안 하면 클라 anon 읽기 0). 9.1과 동일 이슈.
- 클라: `index.html`이 로드 시 `apt_pyeong_blob?id=eq.1` 1회 fetch → `PYEONG_MAP`. `MF_CFG.area` 9프리셋 다중선택(`MF_STATE.area`=버킷배열), `passMapFilter`가 `PYEONG_MAP[d.code]`와 교집합 판정(`PYEONG_READY` 전엔 필터 안 함=안전).
- **실거래 재채워지면 build_pyeong.sql 재실행해야 신규 단지 평형 반영됨.**

### 9.5 ★ 근본 해법 후보 — 국토부 단지코드 매핑 (미착수, 강력 추천)
- 실거래 API 자체엔 단지 고유ID 없음(필드: aptNm/jibun/umdNm/buildYear/sggCd/거래정보뿐). 그래서 이름·주소 fuzzy 매칭이 불가피.
- **국토부_매매·전월세 단지 코드 파일**(data.go.kr/data/15063991) = 단지코드↔단지명·지번·법정동·도로명 매핑. 이걸 받아 우리 21,718개(kaptCode)와 지번+법정동으로 1회 연결하면 **국토부 정확 단지명 확보 → 매칭 정밀도 대폭↑**, 화성 코드·이름 접두 문제 상당수 근본 해결. 규모 있는 작업이라 별도 집중 필요.

### 9.6 실거래 프리페치 (완료, 라이브)
- `index.html` `renderVisibleMarkers` 끝에 `prefetchTradeFiles()` — 핀 그린 뒤 idle(`requestIdleCallback`)에 화면 단지 CDN 파일을 저우선·동시4개 백그라운드 프리페치 → 클릭 즉시(콜드 1.7초→10ms 검증). 핀 렌더와 별개라 속도영향 없음.

### 9.7 배포/상태
- 배포 완료 커밋: `70fc095`(프리페치+평형필터), `8d2a568`(괄호매칭+자가치유), `810e31f`(trade.js 형제코드), `2a9aaa2`(warm.js 개조). 전부 푸시·배포됨.
- 재워밍 재가동 중(cursor 0부터, ~3일). `a99-warm-progress-alert`(매일 9:05)로 진행률 통지 — 이제 정상적으로 0%→100% 추적.
- `a99-mgmtcost-health-check` 활성.
- 완료 후: `build_pyeong.sql` 재실행하면 새로 채워진 단지 평형 반영.

---

## 1. 광고 (AdFit)

- **반려 원인 제거** — 지도 우측 하단 "닫기형" 코너 광고 삭제. 광고 위 "닫기" 버튼이 오클릭 유발로 매체 심사 반려였음 → 삭제 후 **매체 승인**됨.
- **아파트 상세페이지(`api/aptpage.js`)**: 좌·우 사이드 160×600 + 하단 300×250 + **최상단 얇은 배너(320×100, `AD_UNIT_TOP` = DAN-MS50 재사용)**.
  - 최상단 배너: `<body>` 바로 아래 `.top-ad-bar` 전체폭 정적 `ins`. 나중에 PC 728×90 만들면 기기별 분기 가능.
- **검색 페이지(`index.html`)**: 좌·우 160×600 + 하단 300×250(페이지번호 위, 여백 28px).
- **지도 상세 상단 PC**: 죽은 250×250 → 새 300×250(`DAN-W27gieBwJhhY4OlP`). 모바일은 320×100.
- **광고 사라짐 버그 수정**: `a99LoadAd()` 헬퍼 — 새 `ba.min.js` 로드 직전 이미 렌더된 `ins`의 class를 `kakao_ad_done`으로 바꿔 스캔에서 제외(보호). 지도→검색 이동 시 사이드 광고 사라지던 문제 해결.

### AdFit 광고 단위 (전부 "승인")

| 코드 | 사이즈 | 사용처 |
|---|---|---|
| DAN-A9WnwICFDEl16jU1 | 160×600 | 아파트 좌 사이드 + 검색 좌 |
| DAN-6hzBUhx1qcJKOIsS | 160×600 | 아파트 우 사이드 + 검색 우 |
| DAN-VVNFKrpvthFpw6PS | 300×250 | 아파트 하단 + 검색 하단 (본문배너) |
| DAN-KpmCmBSL2hBTN2xb | 300×250 | 지도 상세 하단 |
| DAN-W27gieBwJhhY4OlP | 300×250 | 지도 상세 상단 PC |
| DAN-MS50ssMpQLYwDL5r | 320×100 | 지도 상세 상단 모바일 + 아파트페이지 최상단 배너 |
| DAN-2U9jwSfPfCYLIyyE | 250×250 | **폐기** (fill 안 됨, 코드에서 제거) |

### 광고 gotchas
- **단위 삭제 금지** (여러 페이지 재사용 중). **같은 DOM에 같은 단위 두 번 금지**(다른 페이지 재사용은 OK).
- **250×250 쓰지 말 것**(fill 안 됨). 표준 사이즈(300×250/160×600/320×100/728×90) 사용.
- **새 단위는 실광고/대체광고 뜨는 데 몇 시간~하루** 걸림.
- **광고에 닫기(X) 버튼 금지**(심사 반려). **본인 클릭 금지**(부정클릭 정지).
- "카카오 비즈니스"만 뜨는 건 정상(no-fill 대체광고).
- **새 광고 슬롯 추가 시 반드시 `a99LoadAd()` 패턴** (안 그러면 사라짐 버그 재발).

---

## 2. SEO / 색인

- **홈페이지 메타**(`index.html`): description(80자↓), canonical, og:*, twitter card.
- **브랜딩 a99 → 아구구 통일**(`api/aptpage.js`): 로고 `아구구`, `og:site_name`=아구구, 푸터. `index.html` og:site_name도 아구구.
- **구조화 데이터(JSON-LD)** 단지페이지에 추가: `WebSite`(name 아구구), `BreadcrumbList`(아구구 › 단지명) → 검색결과에 "아구구" 브랜드/브레드크럼 노출 유도.
- **사이트맵 (규격 정비)**:
  - `apt/sitemap.xml` — 전 단지 + `lastmod`
  - `sitemap-main.xml` — 홈/privacy/terms
  - `sitemap.xml` (root) — **사이트맵 인덱스**(위 둘을 묶음)
  - `robots.txt` → root `sitemap.xml` 가리킴
  - 생성기: `node scripts/build_sitemap.js` (데이터 갱신 시 재실행 → lastmod 갱신 → 재수집 유도)
- **단지페이지 내부링크**: "주변 아파트 단지"(같은 동→시군구, 12개) → 크롤러 순회·색인 가속. 앵커 텍스트 "○○ 관리사무소 전화번호"(키워드 매칭).
- **IndexNow (네이버·빙 즉시 색인)** — 신규:
  - 인증키 파일: `3c4fc034da972bac20f024ef003bf1ec.txt` (루트)
  - 제출 엔드포인트: `api/indexnow.js` — `/api/indexnow?secret=<키>[&limit=N]` 방문하면 전 URL을 IndexNow API에 배치 제출. **데이터 추가·수정 시 이 URL 다시 열면 재제출.** (구글은 IndexNow 미지원 → 사이트맵+서치콘솔)
- **서치어드바이저 제출**: 사이트맵을 `https://a99.co.kr/sitemap.xml`(인덱스)로. 웹페이지 수집은 홈+대표 단지 위주.

---

## 3. 기능/UI

- **F5 탭 유지**: 검색/계산기/PRO에 hash 라우팅(`#search` 등). `showPage`에서 replaceState, onload에서 복원.
- **핀 로딩 속도**: 지도 초기화를 `DOMContentLoaded` 즉시 실행(광고 안 기다림). `initMap`은 `if(map)return` 가드.
- **지도 검색창 제거**: "단지명, 주소 검색"(PC 상단바 + 모바일 🔍) 삭제. (검색은 상단 "아파트검색" 탭 사용)
- **지도 상단 필터바(아실 스타일로 개편, 07-24~27)** — 검색창 자리에:
  - 구조: 칩 가로줄 → 칩 클릭 시 **드롭다운 패널**(`#mf-panel`, `mfPanel(key,btn)`)에 **듀얼 슬라이더 + 프리셋 버튼**.
  - **작동(데이터 있음):** 평형(면적 버킷 프리셋), 세대수(듀얼 슬라이더+프리셋), 입주년차(듀얼 슬라이더+프리셋). 유형 칩 아파트/오피스텔.
  - **준비중(데이터 없음 → `.mfc.soon` 회색, 클릭 시 `mfSoon()` 토스트):** 분양권/재건축/재개발, 매매실거래가·평당가격·매매가·전세가·매매전세갭·전세가율·거래회전율·대지지분·용적률·건폐율. (마커에 가격 데이터 없어서 아직 못 켬)
  - 상태: `MF_STATE={area,units,age}`. 필터 로직: `passMapFilter(d)` → 마커 predicate. 입주년차 = 현재연도 − 준공연도. 세대수/년차 max값(1000/40)은 "이상"으로 처리.
  - CSS: `.mfc`(칩) / `.mfpanel`(드롭다운) / `.mfp-slider`(듀얼) / `.mfp-pre`(프리셋). 색은 브랜드 초록(레퍼런스는 파랑이나 레이아웃만 차용).
  - **왼쪽정렬** + `body:has(#region-sidebar.open) .map-top-bar{left:468px}` → **지역패널 열리면 필터바 자동 우측 시프트**(겹침 방지). 드롭다운 패널은 `left:0`(바 왼쪽 아래).
  - **모바일엔 아직 없음**(map-top-bar 모바일 숨김). 2단계 예정.
- **헤더 오른쪽**: "PRO 중개사" 버튼 → **아이콘+라벨 메뉴 4개**(집내놓기→openSellModal, 중개사→showPage('pro'), 단지제보→openReportModal, 이용안내→openGuideModal). `이용안내` 모달 신규 추가. 모바일엔 숨김(☰ 메뉴 유지).
- **실거래가 차트 툴팁**: 반올림("4.8억") → **전체 금액("4억 8,350만원")**. `fmtPFull()` 추가, 매매/전세/보증금에 적용. Y축은 짧게 유지.
- **파비콘**: 초록 라운드+흰 집. `favicon.ico/.svg/.png(48/180/192/512)`. `index.html`+`aptpage.js`에 link 태그. 삼색 빌딩 대안본 보관: `favicon-alt-buildings.svg/.png`. (버전쿼리 `?v=`는 미적용 — 나중에 교체 편하려면 붙이면 됨)

---

## 3.5 릴스(중개사 영상) 캐러셀 — (07-24 신규, **현재 비활성**)

> **★ 현재 상태: 꺼져 있음.** `index.html`의 `var REELS_ENABLED=false;` 스위치로 **모든 도메인(a99·guhaedo·로컬)에서 릴스 숨김**. 사장님 요청으로 재워둔 상태. **다시 켜려면 `REELS_ENABLED=true` 한 곳만 바꾸면** 아래 기능이 그대로 부활(REELS 맵·렌더코드·CSS 전부 보존).

단지 상세창(`.apt-detail-panel`)에서 단지 이름 **위에** 인스타그램 릴스를 스와이프 캐러셀로 노출.

**구현(`index.html`):**
- HTML: `<div id="adp-reel">`을 `.adp-name` 바로 위에 추가(기본 `display:none`).
- 데이터: `const REELS = { '단지코드': ['릴스코드', ...] }` 맵. **단지코드는 `d.code`** (주의: `d.id` 아님 — id로 매칭하면 안 뜸).
  - 현재 등록: `'A10020284'`(힐스테이트 자이 아산센텀) → 릴스 3개 `['DbHk-AgSA-k','DZxEXCfBaHb','DbDZaEJyvF0']`.
- 렌더: `renderReels(d)` 함수 — `openAptDetail(d)` 안에서 이름 세팅 직후 호출. `REELS[d.code]` 없으면 숨김.
- 임베드: 각 릴스를 `https://www.instagram.com/reel/<코드>/embed/` iframe으로. **영상 다운로드·호스팅 안 함, 주소만 사용.**
- CSS 크롭: `.reel-crop`(overflow hidden, 높이 400px) 안에서 iframe을 `top:-52px`로 위로 밀어 인스타 상단 프로필 헤더·하단 바를 가림.
- 넘기기: `.reel-track`(가로 scroll-snap) + **좌우 화살표 버튼(`.reel-nav`)** + **클릭 가능한 점(`.reel-dot`)**. `goTo(i)`가 `scrollTo`로 이동.

**단지 추가 방법:** `REELS` 객체에 `'단지코드': ['릴스코드1','릴스코드2']` 한 줄 추가하면 끝.

**임베드 방식 한계:** 인스타 iframe은 크로스도메인이라 프로필 헤더 완전 제거 불가(크롭으로 가릴 뿐). **사장님 결정: 당분간 인스타 주소 임베드로 유지.**

---

## 3.6 전문중개사 섹션 (07-27 수정)

단지 상세창의 전문중개사 카드(`renderSpecialistCards`, `index.html`):
- **등록된 중개사 0명 → 섹션 통째로 숨김.** 예전의 빈 "모집 중 / 전문중개사 자리가 있습니다" 자리 카드와 "0/2" 카운트 배지 **전부 제거**.
- **등록된 중개사가 있을 때만** 그 사람 카드 표시(최대 2명). 헤더는 "○○ 전문 중개사" + "지역 전문 중개사에게 매물문의를 해보세요"만.
- (`.spec-empty-*` CSS는 남겨뒀지만 미사용.)

---

## 4. 데이터

- **가짜 데이터 9개 삭제** — `split_output/coords_all_apt.json` 등에서 제거. **전체 단지 21,718**.
- **관리사무소 전화번호**: 21,690개 있음(99.8%). 진짜로 없는 28개는 소형 단지라 급하지 않음.
- **★ 도메인 지식 — 평형대는 "공급면적" 기준이다.** (사장님 강조, 07-29)
  - 전용면적 = 실내 면적 / 공급면적 = 전용 + 주거공용(복도·계단 등). **분양 "○○평형"은 공급면적 기준.** 예: 전용 84㎡ = 34평형(공급 ≈112㎡), 전용 59㎡ = 24평형.
  - **주의:** 현재 평형 필터 버킷 `areaBreak`(`u60/u85/u135/o135`)는 **K-apt 전용면적 ㎡ 기준**(전용 60·85·135㎡ = 국민주택규모 기준선). 그래서 index.html의 평형 프리셋 라벨("~20평대/20~30평/30~40평/40평대~")은 전용 ㎡를 평으로 대충 환산한 것 → **공급 평형과 불일치.**
  - **할 일:** 평형대 표시를 **공급면적 기준으로 환산**하거나 라벨에 "전용 기준" 명시. 관리비 등 "평형별" 추정도 공급면적 기준으로 맞출 것.
  - **★ 사장님 요청(07-29): 평형대 필터를 정확한 9구간으로.** `10평미만 / 10평대 / 20평대 / 30평대 / 40평대 / 50평대 / 60평대 / 70평대 / 80평대~` (+전체). 공급면적 기준.
    - **문제:** 현재 데이터는 전용 4구간(`areaBreak` u60/u85/u135/o135)뿐 → 9구간 불가.
    - **해결 순서 (데이터 프로젝트):**
      1. **실거래가 캐시 워밍 완료** (현재 ~30%, `api/warm.js` GitHub Actions 진행 중). 실거래에 **전용면적 정확값**이 있음.
      2. **단지별 평형대 도출 스크립트**(신규, 예: `scripts/build_pyeong.js`): Supabase `apt_transactions`에서 kapt_code별 거래 전용면적 수집 → **전용→공급평형 환산**(전용률 약 0.74~0.78, 또는 전용㎡ 매핑: 전용59≈24평, 84≈34평, 114≈45평 …) → 단지별로 존재하는 10평 구간 set 저장(파일 또는 Supabase).
      3. **필터 교체:** `index.html`의 `MF_CFG.area`를 9구간 프리셋으로, `passMapFilter`가 도출된 단지별 평형 set 참조하도록. 데이터 없는 단지는 "평형정보 없음" 처리.
    - **주의:** 거래 이력 없는 단지는 평형 못 뽑음(커버리지 = 실거래 있는 단지). 완전 커버는 어려움 → "있는 만큼 정확히" 방향.
    - 현재 4구간 필터(`~20평대/20~30평/30~40평/40평대~`)는 이 작업 완료 시 교체 대상.

---

## 5. 배포/운영/비용

- **git push → Vercel 자동 배포.** 도메인 https://a99.co.kr. (repo: github.com/BILLIONARIE-CAN/guhaedo)
- **`vercel.json`**: `/apt/:code`→`/api/aptpage` rewrite, `/split_output/*` 캐시 헤더, **비-a99 호스트 → a99.co.kr 308 리다이렉트**.
- GA4 `G-B2NJPFNP69`. 네이버·구글 인증 메타 있음.
- **비용**: GitHub 무료, Vercel 무료(Hobby), 도메인 연 1~2만원.

---

## 6. 다음 할 일 / 아이디어 (미구현)

1. ~~단지페이지 콘텐츠 보강 + FAQ 구조화 데이터~~ → **✅ 완료(07-29)**: `aptpage.js`에 소개 문단(`introText`) + FAQ 6문항(`faqSection`) + FAQPage JSON-LD 추가.
2. ~~지역 허브 페이지~~ → **✅ 완료(07-29)**: `/region`, `/region/{시군구코드}` (aptpage.js에 통합, `region-sitemap.xml` 253개, 지역페이지에 광고도 넣음).
3. **백링크**(지식iN·부동산 카페). ← 아직. 사람이 직접. 색인·순위 올리는 제일 큰 지렛대.
4. **번호 없는 28개** 수집(급하지 않음).
5. **★ DAUM(다음) 검색등록 — 미완, 나중에 할 일.**
   - 등록 주소: **register.search.daum.net** → "사이트 검색" → URL `https://a99.co.kr` 입력 → 확인.
   - 그 다음: **[필수] 개인정보 수집 동의 + 서비스이용 동의 체크 → 제목·설명·이름·이메일 입력 → (다음/카카오 로그인) → 등록 신청.**
     - 제목: `아구구 - 아파트 관리사무소 전화번호·실거래가·관리비`
     - 설명: `전국 아파트 관리사무소 전화번호·실거래가·관리비·단지정보를 지도에서 한눈에 찾는 서비스, 아구구.`
   - 반영 3~15일. **다음에 등록하면 네이트(Nate)에도 같이 노출.** (동의·개인정보·제출은 본인이 해야 함)

### ★ 중개사 릴스 등록·검수 + 과금

> **사장님 결정: "관리자 검수 후 등록".** 중개사 제출 → 관리자 확인 → 승인된 것만 게시.

**과금 추천 조합: 월 구독 + 순서 프리미엄**, 단지당 슬롯 2~3개 한정.

### ★ 중개사 플랫폼 (큰 프로젝트 — 나중에)

**추천 스택: Supabase + Vercel.** 빌드 순서: ①DB 스키마 → ②가입·로그인 → ③대시보드 → ④공개 페이지 → ⑤링크 공유 → ⑥관리자 검수.

---

## 6.5 실거래가 캐시 + 워밍 시스템

**구조:** 국토부 API → `api/trade.js` → Supabase `apt_transactions` 캐시.

- **TTL 3단계**: 최근 2개월 24h / 2~7개월 7일 / 7개월+ 영구.
- **캐시 워밍**: `api/warm.js` — 전국 6,048태스크(252시군구×24개월). 진행상황 `warm_state` 테이블.
  - 트리거: `.github/workflows/warm.yml` — GitHub Actions가 20분마다 호출. **완료 후 삭제 가능.**
  - 진행률: `https://a99.co.kr/api/warm?secret=3c4fc034da972bac20f024ef003bf1ec`
  - 현재 진행률: **29.5%** (1786/6048, 2026-07-27 기준)
  - 매일 오전 9시 Claude 스케줄 알림(`a99-warm-progress-alert`)으로 진행률 보고 중.
- Supabase 환경변수: `SUPABASE_URL`, `SUPABASE_KEY`(Vercel Production 등록됨).

---

## 7. 주요 파일 지도

- `index.html` — 지도 SPA. 필터바·헤더·차트·모달·마커. 릴스 캐러셀(`REELS` 맵 + `renderReels`).
- `api/aptpage.js` — 단지 상세페이지 서버렌더(`/apt/{code}`). ★ 07-27: `kapt_info.json` 사용, 관리비 섹션 추가.
- `api/indexnow.js` — IndexNow 제출 엔드포인트.
- `scripts/build_sitemap.js` — 사이트맵 생성.
- `split_output/coords_all_apt.json` — 전국 단지 좌표·정보.
- `split_output/kapt_info.json` — ★ 07-27 신규 예정: 전화번호+팩스+관리비 통합 파일. **아직 미생성(스크립트 실행 전) → 현재는 `kapt_tel.json` 폴백 사용 중, 관리비 섹션 안 뜸.** `collect_kapt_fee.js` 실행 완료 후 생성됨(8절).
- `split_output/kapt_tel.json` — 전화번호+팩스 파일(현재 실사용 중). kapt_info 생기면 폴백으로 전환.
- `scripts/collect_kapt_tel.js` — 전화번호 수집 스크립트.
- `scripts/collect_kapt_fee.js` — ★ 07-27 신규: 관리비 수집 + kapt_info.json 병합 스크립트.

---

## 8. ★ 관리비 수집·표시 시스템 (07-27 신규)

> ### ⚠️ 정정 (07-27 후속) — 배치 방식 폐기, 온디맨드 캐시로 교체
> - 아래 8절의 원래 계획(`collect_kapt_fee.js` 배치 + `kapt_info.json`)은 **엔드포인트가 틀렸었고(존재하지 않는 `getAptMgrcostInfoV4`) 관리비 0개 수집** → 폐기.
> - **올바른 API:** `AptCmnuseManageCostServiceV2` (공용관리비). 오퍼레이션 이름 끝에 **`V2`** 필수(예 `getHsmpCleaningCostInfoV2`). 파라미터 `kaptCode`+`searchDate(YYYYMM)`. 공용관리비가 **17개 항목별 오퍼레이션**으로 쪼개져 있어 한 단지 전체 = 17콜.
> - 17콜 × 21,718단지 = 약 37만 콜(≈37일) → **전량 배치 비현실적.** 그래서 **실거래가처럼 온디맨드 캐시**로 구현함:
>   - **`api/mgmtcost.js`** (신규): `/api/mgmtcost?code=&units=` → 캐시 확인 → 없으면 데이터 있는 최신월 프로브 후 17개 항목 병렬 호출·합산 → **Supabase `apt_mgmtcost`** 저장(30일 TTL=월1회 갱신). 응답 item의 숫자필드를 통째로 합산(필드명 하드코딩 불필요 — 인건비처럼 9필드짜리도 자동 합산).
>   - **Supabase 테이블 생성 필요:** `scripts/apt_mgmtcost.sql` 를 Supabase SQL Editor에 실행(RLS 없음). **이거 안 하면 캐시 저장 안 됨.**
>   - **표시:** `aptpage.js` 단지페이지에 `#mgmt-box` → 클라이언트가 `/api/mgmtcost` 불러 **세대당 월 공용관리비(평균) + "자세히 보기"(17개 항목)** 렌더. 데이터 없으면 섹션 미표시.
>   - 표시값은 **공용관리비**(개별사용료 전기·수도·난방, 장기수선충당금 제외). 나중에 개별사용료/장충금 서비스도 붙일 수 있음.
> - **`collect_kapt_fee.js` 는 실행하지 말 것**(엔드포인트 틀림). 남겨두되 obsolete. `kapt_info.json`도 미사용.
> - K-apt 관리비 3개 서비스: 공용관리비(`AptCmnuseManageCostServiceV2`), 개별사용료, 장기수선충당금 — 키는 셋 다 동일 data.go.kr 키로 활용신청됨(공용관리비는 확인 완료).

---
### (아래는 폐기된 원래 배치 계획 — 기록용)


### 개요

K-apt 관리비 API로 전국 단지 세대당 평균 관리비를 수집 → 평형대별 추정치로 단지 페이지에 표시.

### 데이터 흐름

```
K-apt getAptMgrcostInfoV4
  → 세대당 월 항목 합산 (일반관리비 + 청소 + 경비 + 승강기 ...)
  → avgFee (원/세대) + ym (최근 월)
  + 기존 kapt_tel.json (tel, fax)
  → split_output/kapt_info.json 하나로 병합
```

`kapt_info.json` 구조:
```json
{ "A12345678": { "tel": "041-541-0000", "fax": "041-541-0001", "avgFee": 120000, "ym": "202506" } }
```

### 평형대별 추정 로직 (`api/aptpage.js` → `feeSection()`)

```
㎡당 단가 = avgFee × units / useArea
평형대 추정액 = ㎡당 단가 × 대표면적
```

| areaBreak 키 | 평형대 | 대표면적 |
|---|---|---|
| u60 | 20평 이하 | 49㎡ |
| u85 | 25평형대 | 84㎡ |
| u135 | 33평형대 | 110㎡ |
| o135 | 40평형 이상 | 150㎡ |

- **데이터 없는 단지**: 섹션 자체 미표시 (조건 `if (!avgFee || !useArea || !units) return ''`).
- **비정상 데이터 제외**: ㎡당 단가 100원 미만 또는 2만원 초과는 skip.
- `areaBreak` 구간 세대수가 0인 평형대는 행 미출력.

### 수집 스크립트 실행

```
cd C:\Users\essoz\GitHub\guhaedo
node scripts/collect_kapt_fee.js
```

- 하루 9000개 조회 후 자동 종료. 다음 날 재실행하면 이어서.
- 완료 시 Windows 토스트 알림 + 콘솔 메시지.
- 전국 21,718개 → 약 3일.
- **완료 후**: `kapt_tel.json`은 폴백용으로 남겨두면 됨 (aptpage.js가 info 없으면 자동 폴백).

### 미완료 항목 (검색광고 채팅에서 처리 예정)

- **Claude 스케줄 알림** — 워밍 알림(`a99-warm-progress-alert`)처럼 매일 관리비 수집 진행률을 Claude가 알려주는 스케줄 태스크 추가. (이 채팅이 아닌 **검색광고 채팅**에서 설정할 것)

### 관련 CSS (aptpage.js 인라인)

```css
.fee-box { border: 1px solid #e8f5e9; border-radius: 10px; overflow: hidden; }
.fee-row { display: flex; justify-content: space-between; padding: 10px 14px; border-bottom: 1px solid #f0fdf4; font-size: 14px; }
.fee-row:last-child { border-bottom: none; }
.fee-py { color: #444; }
.fee-py small { color: #aaa; font-size: 11px; }
.fee-amt { font-weight: 600; color: #15803d; }
```

### 전화·팩스 표시 (07-27, `aptpage.js` telBlock)

- 전화 있으면 초록 박스에 `📞 전화 / 관리사무소`, 그 아래 **팩스 있으면 `📠 팩스`**(점선 구분).
- **전화번호 없는 단지 → telbox 자체 미출력**(`(tel ? '<div class="telbox">…' : '')`). 예전 "관리사무소 전화번호 준비 중" 문구 제거.
- 팩스 데이터: `kapt_tel.json`/`kapt_info.json`의 `fax` 필드(전국 97.7% 보유).

### 관련 레이블 스타일 수정 (07-27)

```css
/* 이전 → 이후 */
.tel-sub: font-size 12px → 14px, font-weight 추가(700)
.fax-lab: font-size 11px → 13px, font-weight 600 → 700
```
